## 하네스: AI 양자화 → 임베디드 배포 학습 가이드 제작

**목표:** `guide (1).html`의 단계 구조를 기반으로, Ubuntu 22.04 + NVIDIA RTX 환경에서 실행 가능한 단계별 학습 가이드 MD 세트(`study_guide/`)를 생성/갱신한다.

**트리거:** 양자화·임베디드 가이드 문서의 작성/보완/업데이트/부분수정/재검토 요청 시 `embedded-guide-orchestrator` 스킬을 사용하라. 단순 질문(개념 설명 등)은 직접 응답 가능.

**구성 요소:** 에이전트 `guide-author`(리서치+작성), `tech-reviewer`(검증) / 스킬 `embedded-ai-research`, `stage-guide-writing`, `tech-guide-review`, `md-to-html`(HTML 렌더러) / 오케스트레이터 `embedded-guide-orchestrator`. (상세는 `.claude/agents/`, `.claude/skills/` 참조)

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-27 | 초기 구성 (Fan-out/Fan-in + Producer-Reviewer) | 전체 | 단계별 가이드 MD 제작 하네스 신규 구축 |
| 2026-07-28 | MD 심화 재작성 + `md-to-html` 스킬(HTML 렌더러) 추가 | skills/md-to-html, study_guide/* | 각 단계 상세화 및 HTML 버전 제공 요청 |
| 2026-07-31 | 실측 반영 정정 (부분 재실행: author-1 → reviewer-1) | study_guide/01·02·03·04·05·06·07·09·10, README | 0단계를 실제 머신에 설치해보니 정본 핀 `onnxruntime-gpu==1.28.0`이 미존재, `onnx` IR 상한·cuDNN 경로·`numpy<2`·`onnxscript` 누락이 드러남 → 실측 스택(ORT 1.23.2 / onnx 1.18.0)으로 전 문서 통일 |
| 2026-07-31 | 0.5단계 실습 반영: TensorRT EP LD_LIBRARY_PATH 픽스 (3-4-a 보강, 부분수정) | study_guide/01_environment_setup.md·html, README | 0.5단계(배포 사다리) Lv.3 실습 중 `TensorrtExecutionProvider`가 provider 목록엔 나오지만 `libnvinfer.so.10`(venv의 `tensorrt_libs` 패키지, `nvidia/*/lib` 글롭 밖)을 못 찾아 조용히 CPU로 폴백하는 걸 실측 발견(p50 11.83ms=CPU급 → 픽스 후 0.41ms) → cuDNN과 동일한 LD_LIBRARY_PATH 패턴으로 해결, `verify_trt_ep.py` 검증 스크립트·트러블슈팅 행 추가 |
| 2026-08-02 | 1단계 실습 반영: 정정 10건 (부분수정, author-3·5·9 팬아웃 → reviewer 팬인) | study_guide/03·05·10 (+각 html), README, study_guide/README, logs/stage1_quantization_log.html | 1단계(양자화 이론)를 실제로 완주하니 문서의 경험적 단정 여러 개가 실측과 어긋남 → ① ORT `CalibrationMethod.Entropy`가 기본값(`num_bins=128`)에서 후보 1개로 퇴화해 **MinMax와 비트 단위 동일**(scale 32/32 일치), `num_bins`는 `quantize_static` 화이트리스트 5키 밖이라 **전달 자체가 불가** ② "Entropy/Percentile > MinMax" 통념이 ResNet18에선 반대(MinMax 78.90% 최적, pct99.9 −6.20%p, Entropy 정상화 시 −10.80%p) — 클리핑 비율↔top-1 단조 관계로 대체 ③ ORT 권장 `QUInt8` 비대칭 QDQ를 TensorRT가 파싱 못 해 **무음 폴백**(3.05ms, FP32 0.95ms보다 3배 느림) → 대칭 `QInt8`로 0.55ms(5.5배), **2×2 절제 실험으로 하드 블로커가 zero-point≠0 하나뿐**이고 INT32 bias DQ는 2차 증상임을 확정 ④ layer sensitivity 실측이 "conv1이 취약" 통념을 뒤집음(21개 중 가장 안전, per-channel 전제에선 채널 수가 적을수록 유리) ⑤ opset 17 다운컨버트 실패가 **exit 0**으로 통과, external data 2파일 산출 ⑥ QAT/STE 기대 출력값 정정 + STE 미적용 대조군 추가 ⑦ 1000장 큐레이션 셋의 top-1 부풀림 단서 + paired 검정(McNemar) 도입 |
| 2026-08-10 | 인수인계 문서 + 작업 메모리 사본 + QAT 실험 이관 (직접 수행 — 서브에이전트 미사용) | HANDOFF.md, .claude/memory/, experiments/qat_recovery/, README | 다른 PC에서 작업을 이어받을 수 있게 **상태**를 문서화. ① `HANDOFF.md` 신설 — git에 없는 것(데이터 27GB·venv 14GB·작업 스크립트)의 확보 절차, 진행 중인 QAT 회복 실험 재실행법, 이전 머신 GPU 고장 진단 ② 로컬 메모리 7건을 `.claude/memory/`로 복사 — 🔴 복사 전 스캔에서 **sudo 암호 평문 2곳**이 걸려 `<암호>`로 마스킹, git 자격증명 메모리 1건은 제외, `originSessionId`/`modified` 로컬 메타 제거 ③ QAT 실험 스크립트 2개를 `experiments/qat_recovery/`로 이관 ④ QAT 실험 중 실측: **배치 축소는 GPU 이탈에 무효한 레버**(BS 96→48에서 전력 −8W뿐, 3차 사망), 진짜 시그널은 SW Power Cap 상시 점등 → 다음 레버는 `nvidia-smi -pl` ⑤ 스크립트 버그 3건 수정(대조군 `.eval()` 누락, 하드코딩 상수→JSON 핸드오프, 캘리브 장수가 배치에 묶여 있던 것) |
| 2026-08-06 | 1단계 **50k 재실행** 반영: 정정 12건 + 죽은 링크 3건 + 학습자료 페이지 (직접 수행 — 서브에이전트 미사용) | study_guide/02·03·05·06·08·10 (+전 문서 html 재렌더), README, study_guide/README, learning_resources.html, paper/(gitignore), .claude/skills/md-to-html/scripts/render.py, .gitignore | ImageNet val **50,000장 전량**을 확보해 1단계를 재실행하니 1차(클래스당 1장 큐레이션 1,000장) 수치가 **평균 +9.77%p 부풀려져 있었고 Δ 부호 3건·유의성 판정 5건이 뒤집힘** → ① `10_pitfalls.md`에 **함정 0(평가셋 검정력)** 신설 — SE/CI·표본수 상한·McNemar 스크립트(`eval_power_check.py`)와 부호·판정 반전 실측표 ② **함정 2-b(전처리)** 신설 — squash vs torchvision 전처리 차이 **−1.07%p**가 양자화 대가(−0.12%p)의 **약 9배**, FP32로 공식값(69.758%) 재현 후 양자화를 켤 것 ③ 함정 4에 **CUDA-EP 교차 판정**(`trt < cuda×0.8`) 추가 — "INT8이 FP32보다 느림"만으로는 무음 폴백과 커널 부재를 구분 못 함 ④ `05`: 대칭 강제 대가를 "무료(p=0.52)"에서 **−0.29%p·p=9.2e-5 유의**로 정정, `QuantizeBias`의 정확도 영향은 **정확히 0**(예측 불일치 0장) ⑤ `03`: SQNR↔Δtop-1 **예측력 없음**(Spearman −0.04, worst-3 교집합 0), per-tensor 격차 −1.20%p→**−0.16%p(p=0.0445)**, Entropy 간극 68.62 vs 59.29, 평가 장수를 "목적별"(파이프라인 1,000 / 판정 50,000)로 분기 ⑥ 죽은 링크 교체 — TI `supported_ops_rts_versions.md` 404(→`operators.md`+신설 `quantization.md`, `06`·`08`), LiteRT 양자화 문서 OAuth 무한 리다이렉트(→`developers.google.com`, `02`) ⑦ `learning_resources.html` 신설(사이트 34곳·논문 19편, PDF는 재배포 대신 `paper/fetch_papers.py`) ⑧ 렌더러 `slugify`를 **GitHub 규칙과 일치**시켜 MD 크로스링크 앵커 3건 복구(공백 접기·strip 제거) |
| 2026-08-16 | QAT 회복 실험 완주(W4A8 손실변형) + 보고서 §11 신설 + 인수인계 완결화 (직접 수행 — 서브에이전트 미사용) | experiments/qat_recovery/*(스크립트 2·로그 2·JSON·README), logs/stage1_50k_rerun_reproduction_report.html, HANDOFF.md, CLAUDE.md | 이전 머신 GPU 고장으로 미완이던 QAT 회복 2팔 실험을 정상 RTX 3080에서 완주 → ① 기본 W8A8은 무손실급이라 회복률이 노이즈에 묻힘 → **weight 4-bit 손실변형**(`QAT_WBITS=4` 한 노브만 오버라이드)으로 회복률 최초 측정: FP32 68.51%→PTQ 44.35%(**−24.16%p**)→QAT 67.81% = **97.1% 회복** ② 대조군(fake-quant만 제거한 동일 FP32 파인튜닝)이 순수 추가학습 몫 **+0.80%p**를 분리 → **QAT−대조군 −1.50%p**가 4-bit 환원불가 대가, "QAT=공짜 회복" 반증 ③ 스크립트 2개에 `QAT_WBITS` 노출(팔2는 `(bs,ep,wbits)` 일치검증 추가), 실행 로그 2개·결과 JSON 기록, README "(진행 중)"→완결(결과표·판정 4종) ④ 보고서에 회복 계단·대조군 분해 인라인 SVG 2종 + 판정 4종, 기존 §11 산출물→§12 승번 ⑤ RTX 3080은 QAT를 300W/95%/70°C로 완주(SW Power Cap 점등 없음) → HANDOFF §6 "옮기는 게 해결책" 가설 확인, §1·§5·§7 완주 반영 ⑥ 캐비앗 불변: val split 학습이라 절대 top-1은 문헌 비교 불가, 상대 관계(회복률·대조군 격차)만 유효 |
| 2026-08-16 | 1단계 §2.5에 QAT 회복 실측(W4A8) 반영 (부분수정 — author-3 몫 직접 작성 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/03_quantization_theory.md·html, CLAUDE.md | QAT 회복 실험(커밋 10461f2)의 확정 수치가 보고서 §11엔 있었으나 **가이드 본문엔 없어** §2.5가 "STE가 grad를 통과시킨다"만 합성 텐서로 보이고 "실모델에서 얼마나 되찾나"는 비어 있었음 → ① **§2.5.4 신설**(실모델 QAT 회복, ResNet18) — 회복 계단(FP32 68.51%→PTQ 4-bit 44.35%=**−24.16%p**→QAT 67.81%=**97.1% 회복**)·대조군 분해(동일 학습 FP32 파인튜닝 **+0.80%p** vs **QAT−대조군 −1.50%p**) 표 2종, "회복은 진짜지만 공짜 아님" 판정, "val split→절대 top-1 문헌비교 불가" 캐비앗, `experiments/qat_recovery`·보고서 §11(`#s11`) 크로스링크 ② 왜 손실변형인가: 정본 W8A8은 PTQ 손실 0.06~0.14%p(노이즈)라 회복률이 1116.7%로 무의미 → `QAT_WBITS=4`로 −24.16%p를 만들어 첫 측정 ③ 기존 LSQ를 **2.5.5로 승번**(참고문헌 "2.5.5의 출처"·학습목표 체크리스트 동기화) ④ tech-reviewer 팬인이 필수 수치 15개 정본 1:1 일치·산술 7관계·`#s11` 앵커 실재·승번 오염 0 확인(🔴 0건) |
| 2026-08-16 | **2단계 실기 검증**: DETR INT8 양자화 실측 반영 + 산출물 2종 신설 (부분수정 — author-4 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/04_transformer_quantization.md·html, logs/stage2_detr_quantization_report.html(신규), experiments/stage2_detr/(s2_01~s2_10·onnx_export_failures.md 신규), README, study_guide/README, CLAUDE.md | 712줄 초안(미검증)을 실제 RTX 3080에서 `facebook/detr-resnet-50`으로 완주(export→INT8 PTQ→**COCO val2017 전량 5,000장** mAP) → 초안의 경험적 단정 **3건 반전** ① export 첫 블로커가 통념의 `grid_sampler`가 아니라 **SDPA**(opset14+)임을 4경로 대조로 확정 — DETR엔 grid_sample이 아예 없음(Grid op=0); 겸사 torch 2.11 `torch.onnx.export` 기본이 **dynamo=True**(요청 opset 무시·18 강제·external data 분리)·**timm 필수**(TimmBackbone ImportError) 실측 ② §4.5 헤드라인 "폭락→mixed 회복"을 **반증** — attention score matmul 36개를 FP로 남겨도 mAP 0.2402→**0.2438(+0.36뿐)**; 3/4 초안 제외 패턴이 no-op(GELU 없음·Softmax/LayerNorm은 ORT QDQ가 애초에 양자화 안 함) ③ 절제로 **범인=분산** 확정(transformer만 INT8=0.2391≈full, backbone만 INT8=0.2653, sub-additive) → op-선택 아닌 **activation 양자화 입도**(SmoothQuant/per-token)가 진짜 레버 ④ 폭락 실측: FP32 **0.4207**(공개값 42.0 일치)→INT8 **0.2402(−42.9%)**, 작은 객체 mAP_s **−77%** ⑤ Percentile 캘리브가 동적shape·OOM(31GB)·`inf` **3중 실패** → MinMax만 생존 ⑥ 산출물: 실측 리포트 HTML(§1~8·SVG)·`onnx_export_failures.md`(로그 원문·design rules) ⑦ 캐비앗: 절대 mAP는 CUDA EP·MinMax 기준이라 상대 관계만 유효. SmoothQuant(§4.4)·BEVFormer(§4.6)는 미실행(다음 과제) |
| 2026-08-17 | **2단계 §4.6 실기 검증**: BEVFormer-tiny grid_sample/Deformable Attention 실측 반영 + 산출물 2종 신설 (부분수정 — author-4 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/04_transformer_quantization.md·html, logs/stage2_bevformer_quantization_report.html(신규), experiments/stage2_bevformer/(b01~b09·onnx_export_failures.md 신규), README, study_guide/README, CLAUDE.md | 미검증 §4.6 초안을 실제 RTX 3080에서 `fundamentalvision/BEVFormer` `bevformer_tiny`로 완주 → **2-tier 검증**(정본 venv는 mmdet3d/mmcv 불가 → **무컴파일 레거시 venv**: 프리빌트 휠 mmcv-full 1.7.0/mmdet3d 1.0.0rc6, torch 1.13+cu117이 드라이버 하위호환으로 CUDA 12.8 머신에서 CUDA init) ① **op 단정 반전 0건(초안이 맞음)** — grid_sample 4D=opset16 경계·5D=opset20·ORT 1.23.2 5D 무커널→**무음 CPU 폴백**·TRT rank-4 하드단언(`addGridSample nbDims==4`)·MSDeformAttn 분해=grid_sample×num_levels(140노드) 전부 실측; 정밀화 2(dynamo가 요청 opset 상향·5D를 opset17에 out-of-spec emit) ② **실전 함정 2건 신규** — (a) 바닐라 mmcv 커스텀 op은 **CPU 텐서로만 유효 export**: CUDA export는 출력을 `Constant`로 baked해 `value`/`reference_points`가 graph.input에서 소실(CPU 244노드 vs CUDA 4노드), exit 0·checker PASS인데 **silent-wrong**; (b) 전체 모델 export는 grid_sample이 아니라 **`point_sampling`의 `lidar2img.view(1,B,num_cam,1,4,4)`에서 `RuntimeError` 사망** — lidar2img/can_bus가 img_metas(비텐서)→numpy→new_tensor로 들어오는 **forward 내부 기능적 입력**이라 트레이서가 batch dim 오바인딩; 3층(기능적 입력+시간축 재귀 prev_bev+grid_sample)이라 **바닐라 유효 export 경로 없음**(포크가 텐서화) ③ 폭락 대신 **FP32만 실측**: BEVFormer-tiny(33.52M, ckpt 643/643) nuScenes-mini val **mAP 0.2647/NDS 0.2667** — 단 **81샘플·2scene·3클래스 0.000 고분산 스모크**(공개 0.252/0.354와 mAP만 우연 근접, NDS 벌어짐), 절대값 문헌비교 불가·상대 델타만 ④ **전체 INT8 = 범위 밖(정직한 폴백)**: 유효 export 없음 → 포크 커스텀 op 플러그인 툴체인(TRT 8.5/CUDA 11.6→정본 재빌드) 전제 ⑤ 산출물: 실측 리포트 HTML(§1~6·per-class SVG)·`onnx_export_failures.md`(Tier A 5 + B1/B2/B3-a~c·design rules)·b01~b09 스크립트/JSON; §4.6.4 신설(실모델)·§4.6.1 표 5D 폴백 실측일자 07-31→**08-17(b03)** 정정 ⑥ tech-reviewer 팬인이 수치 1:1·SVG 막대 비례·캐비앗 병기·§번호 오염 0·앵커 slug 일치 확인(🔴 0건, 통과) ⑦ 캐비앗 불변: mini 스모크라 상대 관계만. SmoothQuant(§4.4)는 미실행(다음 과제) |
| 2026-08-17 | **2단계 §4.4 실기 검증**: DETR SmoothQuant(nvidia-modelopt) 실측 반영 + 산출물 2종 신설 (부분수정 — author-4 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/04_transformer_quantization.md·html, logs/stage2_smoothquant_report.html(신규), experiments/stage2_smoothquant/(sq_common·sq_01~04·README 신규), README, study_guide/README, .claude/memory/(신규 메모리+동기화), CLAUDE.md | 미검증 §4.4 초안을 실제 RTX 3080에서 `facebook/detr-resnet-50`·**nvidia-modelopt 0.45.0**으로 완주 → §4.5 폭락이 지목한 "진짜 레버=activation 입도"를 SmoothQuant로 직접 시험. **경로=Design X**(torch fake-quant 자기일관 3원: FP32/INT8_DEFAULT `max`/INT8_SMOOTHQUANT) — SmoothQuant→ONNX→ORT(Design Y)는 §4.5의 SDPA/dynamo export 벽·이중 양자화를 다시 만나므로 회피; 두 프리셋이 **algorithm만 다른**(weight per-ch axis0·act per-tensor 동일) 걸 이용해 순수 SmoothQuant 효과만 격리하는 깨끗한 A/B ① **헤드라인 실측**(COCO val2017 전량 5,000장, `sq_02`): per-tensor INT8 폭락 **0.4209→0.3301(−0.0908)** 을 SmoothQuant(α=1.0)가 **0.3845 = gap의 59.9%(+0.0544 mAP)** 회복, mAP_s도 0.1255→0.1704 → §4.5 op-선택 mixed(+0.0036)의 **약 15배**로 §4.5 판정 4("op 선택 아닌 activation 입도가 레버") **실측 확증** ② **초안 오류 3건 정정** — (a) α 프리셋 기본은 논문의 0.5가 아니라 **1.0**(문자열 프리셋→빈 kwargs→`smoothquant(alpha=1.0)`, 설치본 `model_calib.py:954` 라인 단위 실검증); DETR에선 α=1.0(gap 66.6% 회복)이 α=0.5(49.8%)보다 나음(`sq_04`, 500장 별도 스윕) (b) dict-override(`cfg["algorithm"]={"method":"smoothquant","alpha":x}`) **유효** 실증(override_worked=true; `model_quant.py:511`·`mode.py:377-380` 실검증) (c) absmax 삽화값 31.7×→2.96× → 실측 k_proj **3.69×→1.96×**(`sq_03`) ③ **캐비앗**: 절대 mAP는 torch fake-quant 경로라 커밋된 §4.5 **ORT QDQ 절대값**(0.4207/0.2402)과 1:1 비교 불가(그래서 INT8_DEFAULT drop −0.0908이 §4.5 ORT −0.1805보다 얕음), 상대 관계만 유효 ④ 산출물: 실측 리포트 HTML(§1~7·삼원 mAP SVG width=mAP×1200)·`experiments/stage2_smoothquant/`(sq_common+sq_01~04 스크립트/JSON/README) ⑤ **tech-reviewer 팬인 통과**(🔴1·🟡1 리뷰어 직접 수정·🟢3): 수치 SSOT 1:1 전건·산술 5관계·SVG 비례(505.1/396.1/461.4)·앵커/§번호 오염 0 확인; 🔴 smooth_check 스니펫 주석의 "30배→3배면 성공" 통념이 실측(3.69→1.96)과 충돌해 오독 유발 → 모델 의존 명시로 교체(§4.4 L475), 🟡 §2.2.3 삽화값 스코프 병기(L160); 🟢① 소스 라인 3건은 설치본 modelopt 0.45.0 대조로 **라인 단위 실검증 완료**, 🟢③ SVG 라벨 클리핑은 리포트 viewBox 720→800으로 해소 ⑥ 이로써 2단계 실기 3종(DETR §4.5·BEVFormer §4.6·SmoothQuant §4.4) 완결. 커밋·푸시는 규약대로 요청 시만 |
| 2026-08-17 | **3단계 실기 검증**: TensorRT 10.16 pip 휠 실측 반영 + 산출물 2종 신설 (부분수정 — author-5 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/05_tensorrt.md·html, logs/stage3_tensorrt_report.html(신규), experiments/stage3_tensorrt/(t01~t04·t3_common·parser_constraints.md·README 신규), README, study_guide/README, .claude/memory/(신규 메모리+동기화), CLAUDE.md | 1135줄 초안(전부 `trtexec` 기반)을 실제 RTX 3080 + TensorRT **10.16.1.11**(pip 휠 tensorrt-cu12)로 완주(모델 torchvision ResNet50, 지연 배치1·정확도 val 5,000장) → ① **헤드라인 반전**: 정본 pip 휠에 **trtexec 실행파일이 없음**(PATH·파일시스템 0건) → 문서의 모든 `trtexec --int8` 명령이 실행 불가 → **polygraphy 0.50.3 Python API**(`network_from_onnx_path`+`engine_from_network`+`CreateConfig`)로 동일 3점(FP32 1.6615ms→FP16 0.8459ms **×1.96**→INT8 0.7843ms **×2.12**, −0.52%p, 엔진 122.3→49.2→25.5 MiB) ② **§2.2.1 경로 병기 정밀화**(반전 아님): 1단계 ResNet18 ORT-EP는 "TRT 폴백 원인=activation zp≠0 하나뿐"이었으나, polygraphy/trtexec **직접 파서**엔 하드 블로커가 **둘** — INT32 bias DQ(case B: 대칭·zp=0인데도 parse❌, `act_zp_nonzero_frac=0.0`인데도 죽음)와 zp≠0(case C: `shiftIsAllZeros`, zp≠0 0.213) 독립. 차이는 경로(ORT-EP가 파서 전 QDQ 수술로 bias DQ 흡수), 1단계 결론은 그 경로서 유효 → 5케이스 절제(A~E)로 파서 vs 빌더 축 분리(D: stem conv1 3ch7×7은 parse✅ **build❌** Error10 "no implementation" → `nodes_to_exclude=["/conv1/Conv"]`; E: DETR 실제 두 블로커 동시, zp≠0 0.831) ③ **implicit 생존**: deprecated `IInt8EntropyCalibrator2`가 TRT 10.16서 **빌드 성공**(경고 134건=strong-typing/10.12 표시 126 + explicit/10.1 표시 8, TRT 로그 0), 이 모델선 explicit보다 빠르고(0.7074ms **×2.35**) 정확(76.80%)—TRT가 층별 정밀도 자동선택(INT8 57층 vs explicit 강제 74층), 그래도 신규는 explicit 권장 ④ **범위 밖 정직 폴백**: DLA(num_DLA_cores=0 dGPU)·IPluginV3(툴체인 헤비)·modelopt.onnx(importable=false, [onnx] extra 누락→modelopt.torch만 가용) ⑤ 산출물: 실측 리포트 HTML(§1~8·지연 SVG factor 288.9px/ms)·`parser_constraints.md`(로그 원문+설계규칙)·t01~t04 스크립트/JSON; §2.2.1·실습1·3·5·§6·§7에 삽입만(승번 0) ⑥ tech-reviewer 팬인이 **🔴 1건 직접 수정**(§2.2.1 삽입부 L168 "B가 핵심 **반증**이다"→"핵심 **정밀화 지점**이다": 바로 다음 줄의 "반전 아님/경로 병기" 프레이밍과 **자기모순**이었음 — "반증"은 1단계 ORT-EP 결론이 틀렸다는 함의라 병기 프레이밍과 충돌, 기술내용·에러로그는 보존) 후 재렌더 + 수치 1:1(t04 배열 재카운트 134=126+8·t03 5케이스 zpfrac 0.213/0.831·SVG factor 288.9)·캐비앗·§오염 0 확인; 자체 독립 재검증(같은 JSON 대조)도 일치. 남은 🟡: §2.2.1 내부 case(A~D)와 삽입 case(A~E)의 라벨 충돌(같은 "C"가 상반 의미 — §2.2.1 수식이 붙어 저위험, 미수정) ⑦ 캐비앗: 절대 지연·top-1은 CUDA·polygraphy·배치1 기준 상대 관계만 유효, 5,000장 서브셋 top-1 부풀림(1단계 함정 0). 커밋·푸시는 규약대로 요청 시만 |
| 2026-08-18 | **4단계 CPU 폴백 프록시 실측**: Raspberry Pi 5(Cortex-A76) ARM 폴백 바닥값 실측 반영 + 산출물 2종 신설 (부분·프록시 — author-6 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/06_multi_soc.md·html(§2-2 신설), logs/stage4_arm_cpu_fallback_report.html(신규), experiments/stage5_infrastructure/cpu_proxy/(results 4·raw 4·rpi_bench.py·README 신규), README, study_guide/README, .claude/memory/(신규 메모리+동기화), CLAUDE.md | 사용자 요청 실기(home-rpi Raspberry Pi 5)로 4단계 벤더 NPU(보드 대기) 대신 세 SoC 공통의 **ARM Cortex-A 폴백 경로**를 Cortex-A76 프록시로 실측 → ① **헤드라인 부호 반전**: 같은 3·5단계 자산 ResNet50 INT8 QDQ ONNX·같은 ORT `CPUExecutionProvider`인데 **CPU ISA가 양자화 이득의 부호를 결정** — Pi 5 Cortex-A76(`asimddp`/SDOT INT8 dot-product **있음**)는 INT8 **144.9519→79.0827ms ×1.83 빨라지고**, x86 i9-10900K(Comet Lake, **VNNI 없음**·AVX2까지)는 dot-product 가속 부재 + Q/DQ 오버헤드로 **9.2765→16.3376ms 1.76× 느려짐**(MLAS가 정수 dot-product SDOT/VPDPBUSD 있으면 INT8 가속·없으면 못 함) → "양자화 이득은 하드웨어 의존"을 CPU 축에서 직접 실증 ② **크로스플랫폼 예측 동일성은 FP32에서만**: raw `pred_cls` 1000개 x86↔ARM 1:1 → **FP32 1000/1000(100%)·INT8 958/1000(95.8%, 42장 상이)**, top-1 FP32 양쪽 0.7620·INT8 x86 0.7530/ARM 0.7500 → 정수 누산·재양자화 반올림이 ISA 민감, INT8 정확도 비트 단위 기대 금지 ③ **부수 발견 — golden은 TRT 전용**: `test_matrix_matches_golden`(atol/rtol 1e-3)은 결정론적 TRT 전용이라 비결정 CPU wall-clock 행(Pi 145ms) 재현 불가·shape (10,)vs(6,) 실패 → CPU 4행을 golden-glob되는 `bench/results/`가 아니라 신설 `cpu_proxy/results/`로 격리, 커밋 하네스 **6행·pytest 3 passed 유지**(임계값 2 테스트는 baseline inner-merge라 무영향) ④ **정직 폴백**: Pi는 ARM Cortex-A 폴백 프록시일 뿐 자동차 NPU(QCS8550/RZ-V2H/TDA4VM) 아님(가속 수치 전이 불가) — 측정=offload 0% 바닥값, NPU 본실측은 보드 대기(4-A~C, 이 바닥값이 "가속기가 이겨야 할 최소선") ⑤ 산출물: 실측 리포트 HTML(§1~6·정규화 sign-flip SVG 260px/1.0× — Pi INT8 141.85px < 기준선 400 < x86 INT8 457.91px)·`cpu_proxy/`(results 4·raw 4·이식형 `rpi_bench.py`·README); 06 §2-2 신설(승번 0 — git diff 18 insertions·0 deletions 순수 가산, §3이 뒤따름) ⑥ **tech-reviewer 팬인 PASS(🔴 0·material 🟡 0, 수정 0건)**: 8-JSON SSOT 1:1 전건·크로스플랫폼 순수파이썬 재계산(1000/1000·958/1000)·산술(×1.83=144.9519/79.0827·1.76×=16.3376/9.2765)·SVG rect 4개(260.0/141.85/260.0/457.91)·기준선 x=400 시각논리·크로스링크 실재·§오염 0·회귀 3 passed·캐비앗 3종 병기 확인 ⑦ 캐비앗: 절대 지연·top-1은 CPUEP·wall-clock 배치1·1,000장 서브셋·ORT 버전 상이(Pi 1.28.0/x86 1.23.2) 상대 관계만 유효, Pi↔x86 절대속도는 다른 급 기계라 논점 아님. 커밋·푸시는 규약대로(이번엔 사용자 요청으로 수행) |
| 2026-08-17 | **5단계 실기 검증**: 벤치 하네스(polygraphy Python API) 실측 반영 + 산출물 2종 신설 (부분수정 — author-7 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/07_infrastructure.md·html, logs/stage5_infrastructure_report.html(신규), experiments/stage5_infrastructure/(bench/*·harness_constraints.md·README 신규), README, study_guide/README, .claude/memory/(신규 메모리+동기화), CLAUDE.md | 1229줄 초안(hands-on 미검증)을 실제 RTX 3080 + TensorRT **10.16.1.11**로 완주(모델 torchvision ResNet50, ImageNet val 5,000장 top-1) → 정정 8건 ① **정정 1 trtexec 부재**(3단계와 동일): 정본 pip 휠에 trtexec 실행파일 없음 → 문서의 모든 `trtexec` 명령을 **polygraphy 0.50.3 `TrtRunner`**로 대체, 3점 실측(FP32 1.837ms→FP16 1.0231 **×1.80**→INT8 0.8628 **×2.13**, top-1 0.7688/0.7686/0.768, scratch-mem 8.4/3.9/1.7MB) ② **정정 5 무음 오답(zero-copy 버퍼 에일리어싱)**: `TrtRunner.infer`가 출력 host 버퍼 **하나를 재사용** → `.copy()` 없이 예측을 모으면 전부 마지막 추론을 가리켜 top-1 **0.0014(=1/1000)로 붕괴**; pycuda 원본은 호출마다 `np.empty` 새로 할당 → 이 버그는 **pycuda→polygraphy 치환이 도입**(exit 0 silent-wrong) → `[run(x).copy() ...]`로 0.7688 복원 ③ **정정 6 무음 오답(pivot dropna 자기위반)**: pandas `pivot_table` 기본 `dropna=True`가 all-NaN 스텁행(보드필요 SoC)을 조용히 드롭 → §5-1 "회색행 남긴다" 원칙 위반 → `dropna=False`+`na_rep="보드필요"`; **CSV long-form·회귀 baseline은 6행 보존이라 무영향**(사람이 읽는 pivot만 손실) ④ **정정 2** deprecated `IInt8EntropyCalibrator2`가 10.16서 여전히 빌드(INT8 top-1 0.768) ⑤ **정정 3** 작동 attr은 `device_memory_size_v2`(무접미 deprecated), 값은 실행 컨텍스트 scratch/activation(8.4→3.9→1.7MB)로 **가중치·엔진파일 크기 아님**(3단계 엔진파일 122→49→25MiB) ⑥ **정정 7** `EXPLICIT_BATCH` flag는 10.16서 값=0·DeprecationWarning, 맨 `create_network()`가 이미 explicit ⑦ **정정 4** `data.py` eval-set 정정 ⑧ **정정 8** 문서 "pytest-regressions v3.0+"가 허구 → 실제 최신 **2.11.0**(v3.x 없음), `dataframe_regression` fixture는 2.11.0에 존재하므로 코드는 정상—버전 주장만 틀림 ⑨ 모델 스코핑: 문서의 BEVFormer/mAP 대신 **ResNet50/top-1**(2단계서 BEVFormer INT8 유효 export 없음 확정 → 실제 빌드 가능한 3단계 자산 재사용), `BenchResult.accuracy`는 mAP·top-1 겸용 0~1 ⑩ 산출물: 실측 리포트 HTML(§1~7·SVG 2종: 지연 3-bar 288.9px/ms + `.copy()` 정확도 버그 차트)·`harness_constraints.md`(로그 원문+SSOT 매트릭스+회귀 게이트 데모+범위밖)·bench/(ABC 백엔드·config 러너·pandas 매트릭스·pytest 회귀·6결과 JSON) ⑪ tech-reviewer 팬인 **PASS(🔴 0·material 🟡 0, 수정 0건)**: 6-JSON SSOT 1:1 전건·산술(×1.80=1.837/1.0231·×2.13=1.837/0.8628)·SVG 비례(530.7/295.6/249.3=지연×288.9·.copy() 461.3 vs 0.84)·승번 오염 0(삽입만, §0~9·§4-1~4-10·§5-1~5-3 보존)·크로스링크 6개 실재·캐비앗 병기 확인 ⑫ 캐비앗: 하네스 `_timeit`은 wall-clock(H2D/D2H+파이썬)이라 3단계 event-timed 대비 **절대 지연↑·factor 압축**(×1.80/×2.13 vs 3단계 ×1.96/×2.12), 5,000장 서브셋 top-1 부풀림(1단계 함정 0), 상대 관계만 유효. 커밋·푸시는 규약대로 요청 시만 |
| 2026-08-18 | **4단계 Qualcomm 벤더-NPU 실측**: AI Hub 클라우드로 Hexagon HTP(QCS8550·SA8775P ADP) 실기기 프로파일 + 산출물 2종 신설 (부분·클라우드 — author-6 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/06_multi_soc.md·html(§4-B 콜아웃), logs/stage4_qualcomm_aihub_report.html(신규), experiments/stage4_qualcomm_aihub/(scripts 9·results 7·raw 5·aihub_constraints.md·README 신규), README, study_guide/README, .claude/memory/(신규+동기화), CLAUDE.md | 4단계 CPU 폴백 프록시(offload 0% 바닥값, Pi 5) 위에 **벤더 NPU가 실제로 얼마나 버는가**를 보드 없이 Qualcomm AI Hub 실기기(qai_hub 0.54.0/QAIRT 2.45.0/HTP v73)로 채움 → ① 헤드라인: 3·5단계 자산 ResNet50을 `qnn_context_binary`로 HTP 두 종에 올려 **두 디바이스 모두 100% NPU offload**(깨끗한 CNN·폴백0 — §06 이상형 "Offloaded≈Total" 벤더 실기기 정량 달성), INT8 fp16 대비 QCS8550 **×1.77**(1864→1052µs)·SA8775P ADP **×2.03**(3056→1505µs), execution_cycles 교차확증 ② 🔴 **무음 오답**: 외부 ORT-QDQ 지참 시 compile/profile은 100% offload로 통과해도 **on-device top-1 0.75→0.005 붕괴**(같은 경로 FP32(→HTP fp16)는 0.745 충실 → 범인=HTP 임포트가 외부 QDQ scale 무시, exit0·정상shape·35클래스 집중이라 조용; 동일 ONNX는 4단계 CPU 프록시 x86 CPUEP에선 0.753 정상) → **올바른 경로=AI Hub 자체 `submit_quantize_job`**(HTP-native QDQ)로 **0.735 회복**(FP32 0.745 근접·agreement 0.94), 게다가 748µs·1,985,339cyc·127층으로 외부-QDQ INT8(1052µs·3,754,903cyc·128층)보다 **빠르고 leaner** ③ 툴체인 발견 3종: value_info↔IO 충돌을 ORT/TRT보다 엄격 거부(→`clean_valueinfo_for_aihub.py`)·HTP native fp32 없음(`..._FLOAT_32_converted_..._FLOAT_16` 노드로 fp16 실행)·엄격 NCHW(NHWC 가설 기각) ④ 정직 범위: AI Hub는 Qualcomm 전용 → TI TDA4VM·Renesas RZ/V2H는 보드/툴체인 대기(Pi CPU 프록시 offload0%와 상보) ⑤ 산출물: 실측 리포트 HTML(§1~6·지연 SVG µs×0.18·정확도 SVG top1×600·native 대조 green카드)·`aihub_constraints.md`(로그원문+설계규칙)·scripts9/results7/raw5 ⑥ **tech-reviewer 팬인 PASS(🔴0·🟡1·🟢8)**: 8-JSON+raw+scratchpad배열 SSOT 독립 재계산 전건 일치(×1.77/×2.03 산술·정확도 4경로 배열 재현·raw 사이클 5건 합산·SVG 8막대 비례·§오염 0[21 insert/0 delete]·잡ID 12개·크로스링크·캐비앗); 🟡1(외부QDQ ORT-CPU 값 `0.753`이 200장 격자 밖·미소싱)은 **실제 cpu_proxy x86 CPUEP 1,000장 값으로 출처 명시**해 해소 ⑦ 캐비앗: 절대 지연=on-device estimated_inference_time·배치1, top-1=200장 서브셋(1단계 함정0), native 0.735도 ORT 0.750보다 낮아 **공짜 아님** — 상대 관계만 유효. 커밋·푸시는 규약대로 요청 시만 |
| 2026-08-18 | **8단계 캡스톤 INT8/TRT 관통(후속 세션)**: BEVDet 커스텀 TRT-8.5-plugin 6벽(W1~W6) 통과 + FP32→FP16→INT8 엔진 지연 사다리 실측 + 산출물 2종 신설 (부분·직접 — author-8 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | logs/stage8_capstone_int8_report.html(신규), logs/stage8_capstone_report.html(정정 5곳), study_guide/08_capstone.md·html(🔬 콜아웃 3), experiments/stage8_capstone/(scripts +4·results +2[int8_build/trt_ladder]·capstone_constraints.md 벽5·README 갱신), README, study_guide/README·html, .claude/memory/(신규+동기화), CLAUDE.md | 1차 세션([[stage8-capstone-hands-on]] 커밋 64e4c84)이 "다음 과제"로 남긴 **경로 A1**(`convert_bevdet_to_TRT.py --int8`, §4.6 BEVFormer와 동일한 "포크 커스텀 op 플러그인 툴체인" 벽)을 후속 세션에서 **user-space에 실제 조립해 관통** → ① **진짜 벽은 W3·W5, W6은 벽 아님**: W3 커스텀 `TRTBEVPoolV2` 플러그인을 풀 mmdeploy CMake 없이 **2개 TU(`trt_bev_pool_kernel.cu`+`.cpp`)만 self-contained nvcc/g++ 직접 컴파일**(`libmmdeploy_tensorrt_ops.so` 58,896 B, `TRTBEVPoolV2Creator`=bev_pool_v2 v1 등록); W5 `torch.onnx.export`가 nested `QuickCumsumCuda`(symbolic 없는 CUDA autograd.Function)에서 **`RuntimeError:_Map_base::at`+segfault** → `TRTBEVPoolv2.forward`를 **export 전용 `new_zeros` shim**으로 monkeypatch(symbolic만 `mmdeploy::bev_pool_v2` 노드 emit·실연산은 추론 시 플러그인이 수행 → 엔진 정확도 무영향, 표준 mmdeploy custom-op export 기법); W6 convert의 deprecated `builder.build_engine`가 TRT 8.5.3서 이 네트워크에 segfault=**플러그인 탓 아닌 API 탓** → `build_serialized_network`로 플러그인 레이어 포함 완전 빌드 ② **지연 사다리**(batch1 CUDA event-timed, 15warm+60iter): FP32 **14.680**→FP16 **4.905(×2.99)**→INT8 **2.630ms(×5.58**, FP16 대비 ×1.87), 엔진 **245.0/90.1/46.7MB**(×2.72/×5.24↓), INT8 캘리브 ENTROPY_2·nuScenes-mini 81장 ③ **출력편차 vs FP32**(양자화 오차 전파, 6 head): FP16 corr≥0.9994·rel_max≤0.010(무시)·INT8 corr **0.985~1.000**(height head output_1=0.98528 최저·dim head output_2 rel_max=0.21836 최대) ④ **교차확증**: TRT FP32 엔진 14.68ms가 1차 PyTorch eager 34.06ms보다 **2.3× 빠름**(커널 퓨전·파이썬 오버헤드 제거) ⑤ 온디스크 아티팩트 5종(플러그인58896·onnx176901052·엔진245037266/90060319/46734194) **바이트 단위 SSOT 일치** ⑥ 산출물: INT8 관통 전용 리포트 HTML(§1~7·SVG 2종: 지연 factor40 587.19/196.21/105.19·rel_max factor1000 6막대)·구 FP32 리포트 5곳 포인터 정정(자기모순 0)·`08_capstone.md` §4-3(export shim)/§4-4(후속 관통·지연 인라인)/§9(판정②) 콜아웃 삽입(승번 0)·scripts4/results2·constraints 벽5 ⑦ **tech-reviewer 팬인 PASS(🔴0·🟡1해소·🟢다수)**: 2-JSON SSOT 1:1 독립 재산출 전건(지연·비율·엔진·corr/rel_max)·SVG rect width 비례·승번 오염 0(git diff 삽입 위주, 1 deletion은 기존 "범위 밖" 콜아웃 텍스트 리프레이밍)·크로스링크 실재·캐비앗 병기·`grep 범위밖/다음과제` 잔존 0 확인; 🟡1("FP16 corr ≥0.9995"가 표값 output_1 0.99948과 0.00002 미달)은 **4곳 `≥0.9994`로 통일해 해소** ⑧ **캐비앗 불변**: init 가중치(Baidu-locked)라 **절대 mAP≈0 무의미** — 유효 산출물은 가중치-무관 **지연 사다리·엔진 크기·FP32대비 출력편차**뿐(정식 가중치 확보 시 동일 하네스로 정확도 재실행; 경로 A2/B 미시도), 지연은 event-timed·forward-only·batch1이라 타 단계와 1:1 비교 불가·상대만, 플러그인 .so·엔진·pycuda는 legacy env(`~/bevf-legacy` torch1.13.1+cu117·TRT8.5.3.1) 전용 **정본 emb-ai 오염 0**. 커밋·푸시는 규약대로 요청 시만 |
| 2026-08-18 | **8단계 캡스톤 실기 검증**: BEVDet end-to-end FP32 walking skeleton 관통(제3의 길 user-space cu117 툴체인) + 산출물 2종 신설 (부분·직접 — author-8 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/08_capstone.md·html(§3·§4-1·§4-2·§4-4·§9 🔬 콜아웃 5 + §2-1 각주 config 화해), logs/stage8_capstone_report.html(신규), experiments/stage8_capstone/(scripts 4·results 4·capstone_constraints.md·README 신규), README, study_guide/README, .claude/memory/(신규+동기화), CLAUDE.md | 794줄 초안(미검증)을 실제 AI-LAP RTX 3080에서 관통 — **중간 스코프**(사용자 사전합의 = 실제 FP32 baseline까지, TRT-INT8-plugin 범위 밖, 첫 하드월 자동 폴백) → ① **헤드라인=제3의 길(문서 §3 빈칸)**: BEVDet `bev_pool_v2`(LSS pooling, python fallback 없음) 커스텀 CUDA op은 torch≤1.13+CUDA≤11.7 요구인데 머신 유일 toolkit은 **CUDA 12.8** → `torch._check_cuda_version` MAJOR 불일치(12≠11) **hard-error**(§4.6 BEVFormer와 동일 근본원인). 문서 §3의 2안(Docker cu116 / blackwell 패치)이 **Docker 없고 정본 emb-ai(torch 2.11+cu128) 오염 불가한 이 머신엔 둘 다 막힘** → **user-space cu117 툴체인 손조립**(nvcc V11.7.99[micromamba `cuda-nvcc=11.7`, 완전 프론트엔드 — pip `nvidia-cuda-nvcc-cu11`은 ptxas만이라 불충분] + 실제 `libcudart.so.11.0`[pip `nvidia-cuda-runtime-cu11==11.7.99` — torch 번들 hash-suffix .so는 `-lcudart` 링크 불가] + Python.h[micromamba python3.10 CPATH] + `CUDA_HOME=~/capstone-bev/cuda-home` 조립[bin/include/nvvm→cu117 심링크, lib64 실디렉토리에 `libcudart.so`→pip 런타임]) + `TORCH_CUDA_ARCH_LIST=8.6` → **`bev_pool_v2_ext.so` 9,131,040 bytes 빌드 성공**(핵심=`-lcudart`가 실제 SONAME으로 해결) ② **2차 벽 spconv**: 카메라 전용 BEVDet인데 bundled mmdet3d `detectors/__init__`이 LiDAR-fusion **DAL을 eager import**→spconv 요구 → `spconv-cu117`(2.3.6+cumm 0.4.11) 설치로 해소(6개 `__init__` 다중패치보다 깨끗) ③ **데이터 벽**: `create_data_bevdet.py`가 `v1.0-trainval` 하드코딩+무거운 `create_groundtruth_database` → 3곳 패치본(`v1.0-mini`×2 + gt_db 주석), `PYTHONPATH=$PWD` 필수, 산출 `bevdetv3-nuscenes_infos_{train,val}.pkl`=**323 train/81 val**. **초안 오류: 태그가 `bevdetv2`가 아니라 `bevdetv3`**(dev3.0 실제) ④ **walking skeleton 완주(문서 §9) + 정직한 폴백**: 정식 detection 가중치는 **Baidu Pan 전용**(헤드리스 접근 불가, WebSearch 2회로 미러 못 찾음)=사용자 사전승인 fall-back → `init_r50.pth`(torchvision resnet50 backbone[ImageNet 실사전학습] + `init_weights()` head[랜덤])로 stock `tools/test.py` 관통(81 val) → **mAP 0.0000/NDS 0.0260**(전 클래스 AP 0.000)은 **버그 아니라 예상값**(backbone만 진짜·LSS/BEV/head 랜덤). 검증됨=툴체인·데이터·`bev_pool_v2` CUDA op(GPU 실행)·eval 하네스 exit 0. 안 됨=절대 mAP 문헌대조(공개 ≈ 0.298/0.379). **latency는 가중치 무관 유효**: FP32 forward **p50 34.0603ms**(batch1·CUDA event-timed·N=30, 재실행 34.2944ms 지터 내 일치, peak 420.9 MiB, 44.25M params)가 공식 README **BEVDet-R50 33.3ms(RTX 3090)** 근접 교차확증 ⑤ **INT8/TRT-plugin 범위 밖(정직한 폴백)**: §4-4 3경로 모두 벽(A1 `convert_bevdet_to_TRT --int8`·B DerryHub=커스텀 TRT 플러그인 `TRTBEVPoolv2`를 TRT8.5+CUDA11.x로 빌드[§4.6 포크 플러그인과 동일 벽], A2 ModelOpt QDQ=`bev_pool_v2` 표준 ONNX op 아니라 export 자체 §4-3 벽) → INT8 축은 이미 3·5·4단계서 완결, TRT-8.5-plugin 툴체인 확보 시 다음 과제 ⑥ 산출물: 실측 리포트 HTML(§1~6·SVG 2종: walking-skeleton 파이프라인[green=backbone+하네스/purple=`bev_pool_v2` CUDA/amber=랜덤 init] + latency 교차확증 factor 16px/ms) · `capstone_constraints.md`(5벽+레시피+설계규칙) · scripts4/results4/README; 격리 legacy venv `~/bevf-legacy`(torch 1.13.1+cu117·mmdet3d 1.0.0rc4 bundled·spconv-cu117) 정본 emb-ai **오염 0** ⑦ **tech-reviewer 팬인 PASS(🔴0·🟡1·🟢2)**: SSOT 4-JSON 1:1 전건·산술 독립 재계산(SVG factor 16px/ms→544.96/532.8·**NDS가 §2-2 공식으로 TP-오차에서 0.0260 재유도**·9,131,040/1e6=9.13MB)·승번 오염 0(5 콜아웃 git numstat `10 0` 순수 가산, §4-3·§10 후속 헤더 context 보존)·크로스링크 19개 실재·캐비앗 4종 병기 확인; 🟡1(공개 R50 기준값이 **§2-1 표 0.283/0.350** vs **§4-2 콜아웃 0.298/0.379** 병존 — 각각 다른 config[논문/mmdet3d vs dev3.0 `bevdet-r50.py`] 정당값이나 한 문서 병존)은 리뷰 후 **§2-1 각주에 "config별 상이" 1줄 append로 화해**(각주 텍스트 보존한 순수 가산, 승번 0 불변; §4-2 SSOT 콜아웃은 불변) ⑧ 캐비앗: 절대 mAP/NDS는 init 가중치+mini 81장 무의미(파이프라인 검증용), latency event-timed·forward-only·batch1이라 다른 단계와 1:1 비교 불가·상대만, `bev_pool_v2_ext.so` legacy env 전용. 커밋·푸시는 규약대로 요청 시만 |
| 2026-08-20 | **3·4단계 Jetson AGX Orin 온디바이스 실측**: iGPU 정밀도 사다리 + DLA 오프로드/INT8-전용기 + 성능/전력(tegrastats) + §2-2 A78AE CPU-프록시 4번째 행 반영 (부분·온디바이스 — author-5·6 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/05_tensorrt.md·html(§2.3 🔬 콜아웃), study_guide/06_multi_soc.md·html(§2-2 4행+크로스플랫폼 경로의존 정밀화·**신설 §2-3**), logs/stage3_jetson_orin_ondevice_report.html(신규·커밋 00fd97d), experiments/stage3_tensorrt/jetson_ondevice/(scripts·results·raw·README·constraints, 00fd97d), README, CLAUDE.md, .claude/memory/(신규+동기화) | RTX 3080 프록시(3·5단계)로 불가였던 3·4단계 온디바이스를 실 Jetson AGX Orin Dev Kit(JetPack 6.2.1·**TRT 10.3.0.30**·**2× NVDLA v2**·MAXN)에서 관통 → ① **3·5단계 "trtexec 부재→polygraphy" 해소**: JetPack 동봉 실 `/usr/src/tensorrt/bin/trtexec`(배너 v100300)로 원 명령 그대로(정정: 빌드전용 `--buildOnly`→**`--skipInference`**) ② **3단계 "DLA 범위 밖" 해소**: 같은 ResNet50 5엔진 실측 — iGPU FP32/FP16/INT8(1.9375/1.0293/1.0132ms, ×1.91, 단 작은 Ampere라 **INT8≈FP16 0.984** vs RTX3080 0.927) + DLA FP16/INT8 ③ **DLA INT8 = 성능/와트 챔피언 51.29 inf/s/W**(iGPU INT8 33.16의 **1.547×**·전력 0.511×·지연 1.262×) ④ **DLA는 INT8 전용기**: 배치 INT8·FP16 완전 동일(2/2 오프로드·ForeignNode 2=백본120층+fc·GPU폴백 2층[AVG-pool REDUCE+flatten])인데 **DLA FP16 13.87× 느림**=순수 NVDLA v2 데이터패스 ⑤ **오프로드 계측 증명**: GR3D iGPU 95%→DLA 3~16% 붕괴(전력하네스 부하검출은 GR3D 아닌 전력임계, DLA는 GR3D에 안 잡힘) ⑥ **§2-2 sign-flip 4번째 행**(캐리오버): Jetson A78AE CPU-프록시 38.47→18.22ms **×2.11**(2번째 dotprod-ARM, Pi5 A76 확증; 데이터 커밋 49e30ff) + 크로스플랫폼 발견 **경로 의존 정밀화** — 같은 MLAS SDOT면 INT8 예측 **비트 동일**(Jetson↔Pi5 1000/1000), 다르면 ~96%(결정적 대조: 같은 ORT Jetson↔x86 958 vs 다른 ORT Jetson↔Pi5 1000→정수커널 경로가 인자) ⑦ **신설 06 §2-3**(§2-2 CPU 바닥값의 짝=가속기 천장; 승번 오염 0: 05 11/0·06 27/6[6은 전부 라인수정, 섹션삭제 0]) ⑧ **tech-reviewer 팬인 PASS(🔴0·material🟡0·🟢2)**: summary.json+cpu_proxy 8-JSON SSOT 1:1·크로스플랫폼 raw pred_cls 독립 재계산(1000/958/965/961)·산술 7관계(×2.11·1.547×·0.511×·1.262×·13.87×·0.984·9.15×)·DLA 배치 raw verbose 로그 확인(2 ForeignNode+2 GPU폴백, "6 blocks" 없음)·앵커 실재(05 id·06 2링크·05 역링크)·캐비앗 병기 ⑨ 캐비앗: **DLA INT8은 `--int8` 암묵 캘리브라 지연·전력만 유효, 정확도 미주장**(정확도는 명시적 QDQ iGPU INT8·3단계 RTX·4단계 CPU 프록시); 지연 event-timed·batch1·MAXN 상대만. Jetson은 NVIDIA edge이지 세 자동차 벤더(TI/Qualcomm/Renesas) 아님. 커밋·푸시는 규약대로(이번엔 사용자 요청으로 수행) |
| 2026-08-21 | **3·4단계 Jetson AGX Orin 후속 실측**: iGPU∥DLA 동시부하 + nvpmodel 전력 스윕 반영 (부분·온디바이스 — author-5·6 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/05_tensorrt.md·html(§2.3 콜아웃 보강), study_guide/06_multi_soc.md·html(**신설 §2-4**), logs/stage3_jetson_orin_concurrent_power_report.html(신규·커밋 c03c174), experiments/stage3_tensorrt/jetson_ondevice/(scripts +2·results +8·raw +14, c03c174), .claude/memory/(로컬 동기화) | 커밋 00fd97d 솔로 5엔진 스윕이 남긴 캐비앗 ④("iGPU+DLA 동시부하 미측정")를 실 Jetson AGX Orin(JetPack 6.2.1·TRT 10.3.0.30·2×NVDLA v2·MAXN)에서 닫고 nvpmodel 전력 축 추가 → ① **동시부하 = 공짜 병렬 아님(GPU-폴백 직렬화)**: N개 trtexec 동시·20s·INT8·MAXN. iGPU+DLA0 = ideal 합의 **60.8%뿐** — DLA 27% 붕괴(lat 1.28→4.75ms)·iGPU 87.6% 유지, 범인=DLA 엔진 **GPU-폴백 2층**(GlobalAveragePool REDUCE+flatten SHUFFLE)이 포화 iGPU 큐 뒤 직렬화 ② **DLA0+DLA1(GPU 유휴) = 87.0%(1351.5 qps) 스케일 + 전 구성 최고 perf/watt 66.07 inf/s/W**(폴백층이 빈 GPU서 즉시 실행) ③ **3-way(1197.2) < 2-DLA(1351.5)**: 바쁜 GPU가 두 DLA 폴백 각 −75% 교살 → **설계규칙: 진짜 iGPU∥DLA 병렬은 DLA 서브그래프 GPU-폴백 0층일 때만**(ResNet50 GlobalAveragePool이 깸) ④ **nvpmodel 전력 스윕 = 리더 교차**: MAXN→50W에서 iGPU **−29.4% 급락**(983.9→695 qps) vs DLA **−2.9%뿐** → 50W서 DLA가 iGPU보다 **+8.8% 빠름**; DLA ppw는 iGPU 대비 ×1.55(MAXN)~×1.47(50W) 상시 우위 ⑤ **30W/15W = 재부팅 게이트**(온라인 CPU 코어 축소, 사용자 재부팅 거부)라 미측정·값 50W와 동일 → 리포트 회색 행 폐기 병기 ⑥ 산출물: 실측 리포트 HTML(§1~6·SVG 3종: 동시부하 qps·perf/watt·전력스윕)·scripts +2(concurrent.py·power_sweep.py[root])·results +8(6 conc + power_sweep + 파생 summary)·raw +14(tegrastats) ⑦ 승번 오염 0(05 +4/−3 라인수정·06 +31/−2 §2-4 순수 삽입, 섹션삭제 0) ⑧ **tech-reviewer 팬인 PASS(🔴0·material🟡0)**: 6 conc JSON 독립 재산출로 스케일링 60.78/87.01/47.24%·DLA 붕괴 27.06%·지연 3.713×·전력델타 −29.4/−2.9/+8.8%·ppw ×1.5526/×1.4721 1:1·SVG rect width 비례·앵커/크로스링크 실재·캐비앗 병기(리뷰 후 05 캐비앗 30W/15W 주 추가·리포트 SVG C viewBox 620→660 개선) ⑨ 캐비앗 불변: 절대 qps·지연·전력 event-timed·batch1(전력스윕은 명시 모드) 상대만, DLA INT8은 --int8 암묵 캘리브라 지연·전력만·정확도 미주장, GPU-폴백 직렬화 결론은 모델 의존. 커밋·푸시는 규약대로(이번엔 사용자 요청으로 수행) |
| 2026-08-21 | **3·4단계 Jetson AGX Orin 새 모델 축(DETR)**: iGPU 정밀도 사다리 + explicit-QDQ 파싱 실패(case C) + DLA 파편화 실측 (부분·온디바이스 — author-5 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/05_tensorrt.md·html(§2.3 🔬 콜아웃), logs/stage3_jetson_orin_detr_report.html(신규·커밋 9ef2a58), experiments/stage3_tensorrt/jetson_ondevice/detr/(scripts·results·raw 10[verbose 2 gzip]·README, 9ef2a58), CLAUDE.md, .claude/memory/(동기화) | ResNet50 사다리(커밋 00fd97d) 위에 **트랜스포머(`facebook/detr-resnet-50`)를 같은 보드·같은 trtexec·같은 타이밍 플래그로** 올려 "새 모델 축"을 실측 → 3건 ① **무거운 트랜스포머 ⇒ INT8 산술 이득 부활(모델 의존)**: DETR FP16 ×1.947·INT8(implicit) **×2.742**(FP16→INT8 추가 ×1.408). 같은 작은 Ampere iGPU에서 ResNet50@224는 launch/memory-bound라 **INT8≈FP16(0.984·무이득)**이었으나 DETR@800×1066은 큰 attention matmul로 **compute-bound** → "작은 iGPU는 INT8 무이득"은 iGPU가 아니라 **가벼운 CNN의 성질**이었음(3단계 커밋 00fd97d 관찰을 반증 아닌 정밀화) ② **explicit-QDQ INT8 파싱 실패(3단계 case C 실모델 재현)**: `detr_int8.onnx`(ORT QInt8 zp≠0 1085/1485 + INT32 bias DQ 149개)가 node 0에서 **0.051s만에 `shiftIsAllZeros` 단언 실패** → trtexec 직접 파서의 **accuracy-valid INT8 경로는 QDQ 수술/대칭 재export 없이는 막힘**; implicit `--int8`은 빌드(9.43ms)되나 auto-range라 지연·크기만·정확도 미주장 ③ **트랜스포머가 DLA를 파편화(헤드라인)**: ResNet50에 깨끗한 **2/2** offload를 준 동일 `--useDLACore=0 --allowGPUFallback`가 DETR엔 **326 DLA / 404 GPU-폴백 / 16 ForeignNode**(NVDLA v2 코어당 **16 subgraph 상한**에 정확히 걸림) — 404 폴백=250 SHUFFLE+34 MATRIX_MULTIPLY(동적 matmul)+30 NORMALIZATION(LayerNorm)+… → 16 섬 경계마다 DLA↔GPU 왕복 → **DLA FP16 398.64ms=iGPU FP16의 30× 느림** → **설계규칙: DLA는 CNN 가속기, 트랜스포머는 iGPU로**(동시부하 c03c174의 "폴백 0층일 때만 DLA 이득"을 모델 축으로 일반화) ④ 정확도 미측정(보드에 COCO 없음)·2단계 RTX 값 인용(FP32 0.4207→INT8 0.2402 −42.9%) ⑤ 05 §2.3 🔬 콜아웃 삽입(8 insert/0 delete·**승번 오염 0**)·verbose 배치 로그 2개 **gzip**(무손실·op 재카운트 가능) ⑥ **tech-reviewer 팬인 PASS(🔴0·🟡1해소·🟢1해소)**: detr_summary.json SSOT 1:1·산술(×1.947/×2.742/×1.408·30×)·SVG 비례·case C 로그·DLA 배치 16 ForeignNode 독립 재확인; 🟡1(9~13× "무겁다"가 **지연** 비인데 엔진크기 열 옆이라 오독 유발)→§1 stat·§3 헤더·05 콜아웃 3곳 "지연이 9~13×·엔진크기 아님"으로 정정, 🟢1(MiB 반올림 160.13→160.12·66.35→66.34) ⑦ README 콜아웃 미추가(직전 c03c174 후속과 동일 스코프) ⑧ 캐비앗: 절대 지연·엔진크기 event-timed·batch1·MAXN 상대만, implicit INT8 정확도 미주장. 커밋·푸시는 규약대로(이번엔 사용자 요청으로 수행) |
| 2026-08-21 | **3·4단계 Jetson AGX Orin 정확도 축**: iGPU explicit-QDQ INT8 온디바이스 정확도 + TRT INT8 커널 vs CPU MLAS SDOT 예측 1:1 대조(정수커널 경로의존을 CPU↔가속기로 확장) 반영 + 산출물 2종 신설 (부분·온디바이스 — author-5·6 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/05_tensorrt.md·html(§2.3 🔬 콜아웃), study_guide/06_multi_soc.md·html(§2-3 bullet), logs/stage3_jetson_orin_accuracy_report.html(신규), experiments/stage3_tensorrt/jetson_ondevice/accuracy/(scripts 2·results 7 JSON+labels npy·raw 1·README 신규), CLAUDE.md, .claude/memory/(동기화) | 커밋 00fd97d 솔로 스윕이 매 실측에 달았던 "정확도 미주장" 캐비앗을 닫음 — 저장된 `.plan` 엔진을 4단계 CPU-프록시가 쓴 **같은 ResNet50 1000장 번들**에 돌려 이미지별 예측을 1:1 대조. 결정적 장치: `rn50_gpu_int8.plan`은 CPU 프록시가 ORT에 먹인 **바로 그 `resnet50_int8_qdq.onnx`**에서 빌드돼 **QDQ scale 동일** → 유일 변수 = 정수커널 데이터패스 → ① **accuracy-valid INT8이 온디바이스에서 유지 + CPU 커널을 이김**: iGPU INT8(explicit QDQ, 실 scale) top-1 **0.7620 = iGPU FP32**(1000장 무손실)이며 CPU MLAS INT8 **0.7500**보다 높음 → 05 §2.3 캐비앗의 "정확도는 explicit QDQ iGPU INT8에서 확립"이 **온디바이스로 입증**. 무음 FP32 폴백 아님(자기 FP32와 예측 57장 상이·지연 1.01≠1.94ms) ② **INT8 예측 경로의존이 CPU↔가속기 경계를 넘음(4단계 확장)**: TRT INT8(GPU 정수커널) vs MLAS INT8(CPU SDOT) = **961/1000**(39장 불일치, 같은 QDQ scale·같은 실리콘) → 이 96.1%는 4단계 Jetson↔x86 CPU 교차(958/1000=95.8%)와 **같은 대역**이고 Jetson↔Pi5 100%(같은 MLAS SDOT)가 아님 → **정수커널이 다르면 ~96%, 경계가 CPU↔CPU든 CPU↔가속기든 무관**. 39장 중 TRT 정답 15·MLAS 정답 3·둘다오답 21(net +12 = 0.762−0.750). **FP32는 경로 무관** = iGPU FP32 vs CPU FP32 **1000/1000**(4단계 FP32 100%가 가속기까지 확장) ③ **implicit DLA INT8 = 0.017 붕괴(캐비앗 정량화)**: `rn50_dla_int8.plan`(=`trtexec --int8` 캘리브 없음·자동 레인지) top-1 **0.0170**(near-random), **같은 하네스**가 DLA FP16 0.7610을 주므로 하네스 버그 아님 → 솔로 스윕이 implicit/DLA INT8을 "정확도 미주장"으로 라벨한 이유를 수치로 확증 ④ 부수: iGPU FP16 999/1000(무손실)·DLA FP16 0.7610(998/1000 vs iGPU FP16 = NVDLA v2 fp16 데이터패스 미세차)·**"top-1 불변 ≠ 예측 불변"**(INT8 vs 자기 FP32 57장 플립이나 label 대비 net-neutral, pred_cls 배열이 정직한 산출물) ⑤ 산출물: 실측 리포트 HTML(§1~5·SVG 2종: top-1 사다리 폭=top1×600 + 예측플립 폭=flips×9)·`accuracy/`(`orin_accuracy.py` 온보드 러너[tensorrt Python API + `cuda.bindings.runtime`, batch1 execute_async_v3]·`analyze_accuracy.py` 호스트 교차대조·results[5 per-engine pred_cls + accuracy_summary.json SSOT + meta + rpi_labels.npy]·raw[온보드 stdout]·README) ⑥ 05 §2.3 🔬 콜아웃 **author 순수삽입(8/0)**·06 §2-3 bullet+📄 링크행 확장(2/1, 1=링크행) — 섹션 헤딩 무변·**승번 오염 0** ⑦ **tech-reviewer 팬인 PASS(🔴0·🟡1 리뷰어 직접수정·🟢A~E)**: raw `pred_cls`에서 top-1 7종·일치율 7종·헤드라인 분해(39=15+3+21·net+12)·first_indices를 numpy로 독립 재산출 전건 일치, SVG 비례(457.2/456.6/450.0/10.2=top1×600·351/513/9/18=flips×9·0-flip sliver 정직), 긴 한글 §2-2 슬러그 렌더 bit-identical, 캐비앗 4종·헤딩 무renumber 확인; 🟡1 = 기존 DETR 콜아웃(내가 미작성)의 `실습5 line 876` 참조를 **892로 정정**(876이 `PY` heredoc 종료자에 착지 = 무의미 타깃, 내 +8 삽입이 소프트포인터 드리프트를 8→16행으로 키움, 형제 참조 180/187/193은 정밀 → 리뷰어가 zero-risk 단일토큰 수정+05.html 재렌더, net 05 = 9/1[+1 deletion은 이 리뷰어 수정 한 줄]) ⑧ README 미변경(후속 스코프, 기존 콜아웃의 "정확도 미주장" 캐비앗은 implicit DLA 한정이라 불변·오히려 dla_int8 0.017이 확증) ⑨ 캐비앗: 절대 top-1은 **1000장 서브셋**(1단계 함정0 부풀림)이라 3단계 5000장 RTX와 비교 불가 — **같은 번들 상대 관계**(일치율·INT8 vs FP32·TRT vs MLAS)만 결과, `dla_int8`(0.017)은 implicit 캐비앗 정량화용, QDQ scale은 3단계에서 동결(재캘리브 없음). Jetson은 NVIDIA edge이지 세 자동차 벤더(TI/Qualcomm/Renesas) 아님. 커밋·푸시는 규약대로(이번엔 사용자 요청으로 수행, 커밋 d563439 푸시완료) |
| 2026-08-21 | **3·4단계 Jetson AGX Orin 정확도 축 후속(DETR)**: 대칭 재양자화로 case-C 우회 → explicit-QDQ INT8 정확도유효 엔진 빌드 + 온디바이스 COCO mAP (부분·온디바이스 — author-5 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/05_tensorrt.md·html(§2.3 🔬 콜아웃), logs/stage3_jetson_orin_detr_accuracy_report.html(신규), experiments/stage3_tensorrt/jetson_ondevice/detr_accuracy/(scripts 6·results·raw·README 신규), CLAUDE.md, .claude/memory/(동기화) | DETR 새 모델 축(커밋 9ef2a58)이 explicit-QDQ INT8 **파싱 실패(case C)** 로 "정확도 미측정"을 남겼고 직전 정확도 축(d563439)은 ResNet50만 닫음 → 대칭 재양자화로 **DETR "정확도 미측정" 캐비앗을 닫음**(`facebook/detr-resnet-50`, 같은 보드·같은 trtexec). eval=COCO val2017 head 1000·fixed 800×1066, 보드는 raw logits/boxes만 dump→호스트 pycocotools bbox mAP → ① **두 벽 → 툴체인 언락(빌드 가능)**: (1) case C(zp≠0 `shiftIsAllZeros`)+case B(INT32 bias DQ)를 **대칭 재export**로 제거(`ActivationSymmetric+WeightSymmetric` zp=0 전역→C·`QuantizeBias=False`→B·conv1 제외 case D), (2) **트랜스포머 고유 2차 벽(빌더)**: ORT 기본이 모든 op 양자화→대칭이어도 빌더가 self-attention `Constant_3_output_0_quantized` 거부(`qdqGraphOptimizer::matchQuantizedConstantPluginOrDQ`: "Quantized constant is only allowed before DQ or PLUGIN_V2/V3")→`op_types_to_quantize=[Conv,Gemm]`(weight-bearing op만; attention matmul·LayerNorm·Softmax는 FP16, **2단계 §4.5 정합**) → `detr_int8_sym.onnx`(43.4MB·zp_nonzero=0·int32_bias_DQ=0)로 **9ef2a58서 build-failed였던 explicit INT8이 빌드됨**(44.69 MiB·11.002ms) ② **헤드라인(가설 반전) — case-C 픽스는 툴체인 언락이지 정확도 구제 아님**: accuracy-valid explicit-sym INT8이 **stage2 DETR 폭락을 다른 경로로 재현** FP32 mAP **0.4237→0.2383(−43.8%)**·mAP_s 0.2179→**0.0336(−84.6%)**, stage2(ORT-QDQ·dynamic shape·CUDA EP·5000장) 0.4207→0.2402(−42.9%)·mAP_s −77%와 **교차확증**(TRT/fixed/1000 vs ORT/dynamic/5000 두 경로가 같은 폭락) → **레버는 sym/case-C 픽스가 아니라 activation 양자화 입도**(SmoothQuant 2단계 §4.4가 per-tensor gap의 59.9% 회복); FP16 무손실 +0.0006(0.4243) ③ **implicit `--int8` 메커니즘 정정(작업 중 자기교정한 정합성 버그)**: implicit mAP 0.4073(−3.9%)이 sym(0.2383)보다 높다고 "더 나은 INT8" 아님 — 캘리브 캐시 없음→**데이터 유래 activation 레인지 없음(무통제)** 이라 정확도 미주장, **단 "순수 FP16 폴백"은 아님**(implicit 지연 9.43ms<FP16 13.28ms·엔진 58.76<81.43 MiB → INT8 커널 실제 실행); 우연히 FP16 근처(0.4073) 착지일 뿐, 같은 무통제 경로 companion(직전 ResNet50 DLA implicit)은 **0.017 붕괴** ④ 지연 사다리(batch1 GPU-compute median·MAXN·동일 플래그): FP32 25.85→FP16 13.28(×1.947)→**sym 11.002(×2.35 vs FP32·×1.207 vs FP16, accuracy-valid)**→implicit 9.43(×2.742, 미주장) ms·엔진 160.12/81.43/**44.69**/58.76 MiB ⑤ 산출물: 실측 리포트 HTML(§1~6·SVG 2종: mAP factor 1060·latency factor 18.57)·`detr_accuracy/`(scripts 6[`detr_sym_export`·`detr_prep`·`prep_coco_sub`·`orin_detr_map`·`analyze_detr_map`·`build_summary`]·results·raw·README); 05 §2.3 콜아웃 **순수삽입(8/0)**+HTML 재렌더(1/0)·**승번 오염 0**(heading 무변) ⑥ **tech-reviewer 팬인 PASS(🔴1·🟡3·🟢3 전부 리뷰어 직접수정·재검증)**: raw npz에서 mAP 독립 재계산(`analyze_detr_map.py`)·SSOT byte-identical 재생성·산술(×1.947·×2.35·×2.742·−43.8%·−84.6%)·SVG 비례(factor 1060/18.57)·numstat 8/0+1/0·heading diff 0 전건 확인; 🔴1 = sym 엔진크기 **단위모순**(6곳 "46.9 MiB"[=bytes/1e6 십진MB]가 표·SSOT "44.69 MiB"[bytes/1048576]와 자기모순 → 전부 **44.69 MiB** 통일, 수치회귀 0), 🟡3 = README 잔존 "FP16 폴백" 프레이밍·05 콜아웃 raw `<u>` 리터럴 렌더·리포트 §4.4 슬러그 오류(전부 수정) ⑦ 캐비앗: 절대 mAP는 fixed 800×1066(vs stage2 dynamic)·TRT vs ORT·1000 vs 5000장이라 stage2와 1:1 비교 불가 — **상대 폭락만** 결과(그 상대 폭락이 stage2 교차확증)·1000장 서브셋(1단계 함정0)·implicit 미주장·Jetson≠자동차 벤더. **이로써 DETR "정확도 미측정" 캐비앗 닫힘**; 다음 후보 = DETR에 SmoothQuant/per-token(§4.4)로 activation 입도 개선 후 온디바이스 재측정(폭락 회복 여부). 커밋·푸시는 규약대로(이번엔 사용자 요청으로 수행, 커밋 58ba518 푸시완료) |
| 2026-08-21 | **3·4단계 Jetson AGX Orin 정확도 축 후속2(DETR SmoothQuant)**: activation 입도 레버를 ONNX-레벨 SmoothQuant로 온디바이스 시험 + 산출물 2종 신설 (부분·온디바이스 — author-4 몫 직접 수행 + `tech-reviewer` 서브에이전트 팬인 검증) | study_guide/04_transformer_quantization.md·html(§4.4 🔬 콜아웃), logs/stage3_jetson_orin_detr_smoothquant_report.html(신규), experiments/stage3_tensorrt/jetson_ondevice/detr_smoothquant/(scripts 4·results·raw·README 신규), .claude/memory/(신규 후속5+동기화), CLAUDE.md | 직전 후속(정확도 축 DETR, 커밋 58ba518)이 지목한 "다음 후보"(activation 입도 레버=SmoothQuant §4.4)를 실 Jetson AGX Orin(JetPack 6.2.1·TRT 10.3.0.30·MAXN)에서 실행 — stage2 §4.4는 SmoothQuant로 per-tensor 폭락의 **59.9%**를 회복했으나 **torch fake-quant 경로(Design X)에서만**이고 ONNX→TRT 온디바이스엔 미측정이었음 → 같은 보드·같은 1000장·같은 대칭 QDQ 위에 SmoothQuant를 **ONNX-그래프 레벨로** 얹어 폭락 회복 여부 측정. eval=COCO head 1000·fixed 800×1066·pycocotools(58ba518 재사용 러너·`orin_detr_map.run_engine`) → ① **헤드라인(교차확증 반전) — 온디바이스는 폭락의 ~9%만 회복, stage2의 1/6**: gap=FP32 0.4237−sym 0.2383=**0.1854** 중 α=1.0 **0.2553=9.2%(+0.0170)**·α=0.5 0.2544=8.7%. stage2 §4.4 59.9%(torch)보다 약 6.5배 작다. **근본원인=op 커버리지**(SmoothQuant가 틀린 게 아님) — 온디바이스에서 case-C 파서+quantized-const 빌더 벽(2단계 §4.5) 때문에 유일하게 빌드되는 INT8 엔진이 `op_types_to_quantize=[Conv,Gemm]`로 강제돼 **attention MatMul·LayerNorm·Softmax가 FP16**으로 남음 → INT8이 **Gemm 전용**이라 Gemm 입력 smoothing이 지배 잔차 레버 아님; stage2 torch는 attention Q/K/V/out 포함 **모든 linear**가 INT8이라 크게 먹혔음 → **∴ stage2 §4.4 59.9%는 부정 아니라 "경로·op-커버리지 의존"으로 정밀화** ② **SmoothQuant 방향은 옳다(무효 아님)**: α=1.0(9.2%)>α=0.5(8.7%)로 **stage2 DETR-best α 순서 보존**·폭락이 집중된 작은객체 mAP_s 0.0336→0.0449(gap의 6.1%) 부분 회복·대가는 Mul 95개뿐이라 **거의 지연 중립**(sym 11.002→α=1.0 11.3433ms=+0.3413·엔진 44.69→44.87 MiB=+0.18); activation absmax 최대 **46.699×**(Gemm_4)/중앙 7.485× 감쇠, 진짜 INT8 확인(FP32 대비 logits_corr 0.98258·sym과 logits_absmax 16.43로 실제 다름=무음 sym 복제 아님) ③ **툴체인 교훈**: modelopt.onnx 임포트 불가(`onnxslim` 없음)→ONNX 수동 SmoothQuant(95 Gemm 전부 transB=0, per-input-channel `s=a^α/w^(1−α)`, Mul(A,1/s)+weight 행 스케일=구성상 FP32 정확). **FP32 정확도 게이트는 CPU EP에서** — CUDA EP는 Ampere TF32(10-bit 가수)가 재스케일에 민감해 max_abs≈0.65 허위 발화(정밀도 artifact·배선버그 아님; 순수 numpy 수학 1.4e-14), CPU EP=1.748e-4(α=1.0)/1.450e-4(α=0.5) PASS ④ 산출물: 실측 리포트 HTML(§1~6·SVG 2종: gap 회복률 factor 5.5px/% + 지연 factor 18.57px/ms)·`detr_smoothquant/`(scripts 4[`detr_sq_export`·`orin_detr_sq_map`·`analyze_detr_sq_map`·`build_sq_summary`]·results·raw[build 로그 2·dump·engine_sizes]·README); 04 §4.4 콜아웃 **순수삽입(2/0=승번 오염 0, §4.1~4.6.4 헤딩 불변)** ⑤ **tech-reviewer 팬인 PASS(🔴0·🟡1 fixed·🟢3)**: detr_sq_summary.json SSOT 1:1 전건·raw npz 독립 재계산(`analyze_detr_sq_map.py`가 sq 0.2553/0.2544 + 커밋 baseline 0.4237/0.2383 정확 재현=무드리프트 교차확증)·산술 13관계(gap 0.1854·회복 9.2/8.7%·mAP_s 6.1/5.5%·지연델타 +0.3413/+0.4064·speedup 2.279/2.266·6.51× ≈ "약 6.5배")·SVG 비례(SVG1 329.45/50.60/47.85/33.55/30.25·SVG2 480.03/204.31/210.65/211.86)·엔진 바이트 추적성(`raw/detr_sq_engine_sizes.txt`=47052636/47092836 + v100300 배너)·크로스링크 실재·프레이밍 무결(정밀화≠부정) 확인; 🟡1=엔진크기 델타 단위 불일치(5곳 "+0.19 MiB"[십진MB 0.1935]가 §5 표 "44.69→44.87=+0.18 MiB"와 자기모순 → 리뷰어가 리포트·콜아웃·HTML 5곳 **+0.18 MiB** 통일, numstat 2/0 불변) ⑥ 캐비앗: 절대 mAP는 fixed 800×1066·1000장·TRT라 stage2 5000장·torch와 1:1 비교 불가 — **같은 번들 상대 회복률만**·SmoothQuant는 구성상 FP32-정확(이득은 activation 양자화 조밀화)·1000장 서브셋(1단계 함정0)·Jetson≠자동차 벤더. **이로써 stage2 §4.4 "온디바이스 미측정" + 후속 "SmoothQuant 재측정 다음 후보" 둘 다 닫힘**; 다음 후보 = attention까지 INT8 가능한 툴체인 확보 시 per-token/SQ+attention(현재는 빌더 벽으로 Gemm 전용). README(루트·study_guide) 미변경(직전 DETR 후속과 동일 스코프). 커밋·푸시는 규약대로(이번엔 사용자 요청으로 수행, 커밋 b257a17 푸시완료) |
| 2026-08-31 | **4단계 벤더-NPU 실기기 셋업**: 랩 Raspberry Pi 5에 DEEPX DX-M1(M.2 PCIe NPU) 설치·설정 완료 (직접 수행 — 하드웨어 셋업, 서브에이전트 미사용·가이드 문서 무변경) | .claude/memory/README.md(로컬전용 메모리 제외 주석 확장), CLAUDE.md; (로컬 전용, 미커밋) dx-m1-npu-setup 메모리 신규·MEMORY.md 포인터·lab-rpi-board-access 상호링크 | 사용자 요청으로 랩 Pi 5(Cortex-A76·Debian 13 trixie·커널 6.18.34·Python 3.13)에 물리 장착된 DEEPX DX-M1 벤더-NPU를 `dx-all-suite`(github.com/DEEPX-AI/dx-all-suite)로 사용가능화 → ① **dx-runtime 서브모듈만** recursive init(dx-compiler는 x86 전용이라 Pi 제외) → `install.sh` 단계 실행(`--target=dx_rt_npu_linux_driver`[DKMS] → 검증 → `--target=dx_rt,dx_fw --skip-uninstall`) ② **최대 리스크였던 커널 6.18 호환 해소**: 드라이버 소스가 RPi 커널 6.18.34+rpt-rpi-2712에 클린 컴파일(사전 no-root 컴파일 게이트로 확인 후 진행), DKMS `AUTOINSTALL=yes`로 커널 업데이트 시 자동 재빌드(모듈 `dx_dma`+`dxrt_driver`) ③ **Python 3.13 지원**: Debian 13 기본 python3.13 그대로, DEEPX가 `dx_engine-3.4.2-cp313` aarch64 휠 제공(deadsnakes PPA[Ubuntu 전용] 정당 스킵) ④ **검증(재부팅 생존)**: `dxrt-cli -s` = Device 0 M1 Accelerator·**FW v2.7.4**·RT드라이버 v2.6.0·PCIe드라이버 v2.5.0·LPDDR5 3.92GiB·PCIe Gen2 X1·**NPU 3코어 @1000MHz/37°C**; 재부팅 후 드라이버 자동로드(`dxrt.service` enabled+active·`/dev/dxrt0`)·`sudo ldconfig` 후 라이브러리 캐시 정상 ⑤ **가이드 관련성**: 4단계 벤더-NPU 축의 **실 온디바이스 DEEPX 실리콘**(클라우드 Qualcomm AI Hub Hexagon HTP·CPU 폴백 바닥값 Pi5 A76과 상보 — 같은 A76이 CPU 프록시이자 DX-M1 호스트라 CPU↔NPU 동일보드 대조 가능) ⑥ **범위·캐비앗**: 설치·검증만 완료(벤치·모델 실행 미수행 — 별도 요청 시); 실추론엔 `.dxnn` 컴파일 모델 필요(컴파일러 x86 전용이라 Pi에서 컴파일 불가) ⑦ **보안**: 설치 메모리는 내부 IP(lab-rpi 접속 메모리 참조) 때문에 **로컬 전용**(jetson 보드 접속 메모리와 동일 정책) → 공개 레포엔 README 제외 주석 + 이 이력 행만(내부 IP·SSID·키경로·암호 무기재), 설치 중 임시로 넣은 NOPASSWD sudo 드롭인 제거·재부팅 후 암호 sudo 복원 확인. 커밋·푸시는 규약대로(이번엔 사용자 요청으로 수행) |

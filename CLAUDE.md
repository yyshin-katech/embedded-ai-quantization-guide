## 하네스: AI 양자화 → 임베디드 배포 학습 가이드 제작

**목표:** `guide (1).html`의 단계 구조를 기반으로, 실행 가능한 단계별 학습 가이드 MD 세트(`study_guide/`)를 생성/갱신한다. 개발기는 Ubuntu + NVIDIA RTX이고, 4단계 이후는 실기 타깃(Jetson AGX Orin · Raspberry Pi 5 + DEEPX DX-M1 · Qualcomm AI Hub 클라우드 · x86 CPU)으로 확장되어 있다.

**트리거:** 양자화·임베디드 가이드 문서의 작성/보완/업데이트/부분수정/재검토 요청 시 `embedded-guide-orchestrator` 스킬을 사용하라. 단순 질문(개념 설명 등)은 직접 응답 가능.

**구성 요소:** 에이전트 `guide-author`(리서치+작성), `tech-reviewer`(검증) / 스킬 `embedded-ai-research`, `stage-guide-writing`, `tech-guide-review`, `md-to-html`(HTML 렌더러) / 오케스트레이터 `embedded-guide-orchestrator`. (상세는 `.claude/agents/`, `.claude/skills/` 참조)

**공통 규약:**
- **커밋·푸시**는 스캔 후 요청 시만 수행. 실기 실측 이력 다수는 사용자 요청으로 수행됨(커밋 해시는 `HISTORY.md` 각 행에 기재).
- **실기 실측 캐비앗(불변)**: 절대값(지연·top-1·mAP)은 배치1·서브셋·경로 상이라 **상대 관계만 유효**. 각 행 캐비앗은 개별 특이사항만 기재.
- **부분수정 절차**: 해당 author 몫 직접 수행 → `tech-reviewer` 서브에이전트 팬인 검증(수치 SSOT 1:1·산술·SVG 비례·§승번 오염 0·캐비앗 병기). 결과는 🔴/🟡/🟢 건수로 요약.
- **수치의 정본은 이 파일이 아니다**: 각 실기 작업의 산출물은 실측 리포트 HTML(`logs/`) + 실험 스크립트/결과(`experiments/`) + 필요 시 로컬 메모리다. 세부 수치가 필요하면 표를 인용하지 말고 리포트를 열어라. 어긋나면 리포트가 이긴다.

**저장소 지도:** `study_guide/`(가이드 MD 11 + HTML) · `logs/`(실측 리포트 32) · `experiments/`(단계별 스크립트·결과 9) · `publication/`(논문 1 영문·한국어판) · `HANDOFF.md`(머신 인계) · `learning_resources.html`(학습자료)

**현재 상태 (2026-09-16):** 가이드 0~8단계 실측 반영 완료. 논문 1 **arXiv:2609.16085** 공개(primary `cs.AR`), 아티팩트 스냅샷 태그 `paper1-v1`. 미해소 후속 2건은 로컬 메모리(`arxiv-paper1-submission-status`) 참조.

**변경 이력:** 아래는 인덱스다. **전문(절제 실험·방법론·tech-reviewer 판정·행별 캐비앗·커밋 해시)은 [`HISTORY.md`](HISTORY.md)** 에 있다.

| 날짜 | 작업 | 핵심 결론 |
|------|------|----------|
| 2026-07-27 | 하네스 초기 구성 | Fan-out/Fan-in + Producer-Reviewer 신규 구축 |
| 2026-07-28 | MD 심화 재작성 + `md-to-html` | 각 단계 상세화 및 HTML 버전 제공 |
| 2026-07-31 | 0단계 실설치 반영 (정정) | 정본 핀 `onnxruntime-gpu==1.28.0` 미존재 → 실측 스택(ORT 1.23.2 / onnx 1.18.0)으로 전 문서 통일 |
| 2026-07-31 | 0.5단계 TRT EP `LD_LIBRARY_PATH` 픽스 | `libnvinfer.so.10` 미해결로 무음 CPU 폴백(11.83 → 0.41ms) |
| 2026-08-02 | 1단계 양자화 이론 실습 (정정 10건) | ORT `Entropy`가 기본값서 MinMax와 **비트 단위 동일**; TRT 무음 폴백의 하드 블로커는 zero-point≠0 하나뿐 |
| 2026-08-06 | 1단계 **50k 재실행** (정정 12건) | 1,000장 수치가 평균 **+9.77%p 부풀림**·Δ 부호 3건 반전 → 함정 0(평가셋 검정력) 신설 |
| 2026-08-10 | 인수인계 문서 + 메모리 사본 + QAT 이관 | 배치 축소는 GPU 이탈에 무효, 진짜 시그널은 SW Power Cap |
| 2026-08-16 | QAT 회복 실험 완주 (W4A8) | PTQ −24.16%p를 **97.1% 회복**하나 대조군 분리 시 −1.50%p는 환원불가("공짜 회복" 반증) |
| 2026-08-16 | 1단계 §2.5.4에 QAT 실측 반영 | 합성 텐서만 보이던 §2.5에 실모델 회복 계단·대조군 분해 추가 |
| 2026-08-16 | **2단계 DETR INT8 실측** | 폭락 −42.9%의 범인은 op-선택이 아니라 **분산**(activation 입도); export 첫 블로커는 통념의 `grid_sampler`가 아닌 **SDPA** |
| 2026-08-17 | 2단계 §4.6 BEVFormer `grid_sample` | op 단정 반전 0건이나 바닐라 유효 export 경로 없음(3층 벽) |
| 2026-08-17 | 2단계 §4.4 DETR SmoothQuant | gap의 **59.9% 회복** = op-선택 mixed의 ~15배; α 프리셋 기본은 논문 0.5가 아니라 **1.0** |
| 2026-08-17 | **3단계 TensorRT 10.16 pip 휠** | 정본 pip 휠에 `trtexec` **없음** → polygraphy API; 직접 파서엔 하드 블로커 **둘**(INT32 bias DQ·zp≠0) |
| 2026-08-17 | 5단계 벤치 하네스 (정정 8건) | `TrtRunner.infer` 출력 버퍼 재사용 → `.copy()` 없으면 top-1 **0.0014 무음 붕괴** |
| 2026-08-18 | **4단계 CPU 폴백 프록시** (Pi 5 A76) | **CPU ISA가 양자화 이득의 부호를 결정**: A76(SDOT) ×1.83 빠름 vs x86(no-VNNI) 1.76× 느림 |
| 2026-08-18 | 4단계 Qualcomm AI Hub HTP | 외부 QDQ를 **무음 무시** → top-1 0.75 → 0.005 붕괴; HTP-native QDQ로 0.735 회복 |
| 2026-08-18 | 8단계 캡스톤 FP32 walking skeleton | 제3의 길 = user-space cu117 손조립; init 가중치라 mAP 0은 버그가 아니라 예상값 |
| 2026-08-18 | 8단계 캡스톤 INT8/TRT 관통 (후속) | 진짜 벽은 W3(플러그인 직접 컴파일)·W5(export segfault); FP32 → INT8 **×5.58** |
| 2026-08-20 | **3·4단계 Jetson Orin 온디바이스** | DLA INT8 = perf/watt 챔피언(×1.547)이자 **INT8 전용기**(DLA FP16은 13.87× 느림) |
| 2026-08-21 | Jetson 동시부하 + 전력 스윕 | 진짜 병렬은 **DLA 서브그래프의 GPU-폴백이 0층일 때만**(iGPU+DLA = ideal의 60.8%) |
| 2026-08-21 | Jetson 새 모델 축 (DETR) | **DLA는 CNN 가속기, 트랜스포머는 iGPU**(326 DLA / 404 GPU-폴백 파편화) |
| 2026-08-21 | Jetson 정확도 축 | explicit-QDQ INT8 0.7620 무손실 > CPU MLAS 0.7500; TRT↔MLAS 예측 961/1000 |
| 2026-08-21 | Jetson DETR 정확도 (대칭 재양자화) | case-C 픽스는 **툴체인 언락이지 정확도 구제 아님**(−43.8%로 2단계 폭락 교차확증) |
| 2026-08-21 | Jetson DETR SmoothQuant | 온디바이스는 폭락의 **9%만 회복**(2단계의 1/6) — 근본원인은 op 커버리지(Gemm 전용) |
| 2026-08-31 | 4단계 DEEPX DX-M1 셋업 | 커널 6.18 DKMS 호환 해소, FW v2.7.4·NPU 3코어 검증(재부팅 생존) |
| 2026-08-31 | DX-M1 벤치 | 처리량 ×11.42지만 **host-bound**(천장은 Pi5 PCIe Gen2×1 — D2H 21.8ms ≫ 연산 8.86ms) |
| 2026-08-31 | DX-M1 정확도 축 | 외부 QDQ를 **시끄럽게 거부**(Qualcomm의 무음 무시와 정반대 실패양식); CPU↔NPU 939/1000 |
| 2026-09-01 | DX-M1 크로스오버 축 | regime을 정하는 건 연산이 아니라 **출력(D2H) 크기** |
| 2026-09-01 | DX-M1 검출 축 (yolo26n) | 검출 INT8 무손실급(−2.0%); **NMS-fold로는 레짐 못 바꿈**(툴체인에 그 길이 구조적으로 없음) |
| 2026-09-02 | DX-M1 DETR 트랜스포머 축 | 무폭락은 트랜스포머를 **양자화 안 해서**(컴파일러 자동분할 → 호스트 FP32); 제3 레짐 host-CPU-bound |
| 2026-09-09 | DX-M1 전이 곡선 + **첫 논문 반영** | 경계는 knife-edge가 아니라 **밴드**, 폭 = 코어 수(3.36× ≈ 3, N코어가 D2H 링크 1개 공유) |
| 2026-09-09 | 논문 1 v0.3 정합성 3배치 + PoT 스캐폴드 | C2는 "구배"가 아니라 **불변**: 정수 커널 공유 = 1000/1000, 아니면 958~965(이진) |
| 2026-09-10 | **PoT 완화책 실측 → NO-GO** | 주 게이트 958 → **869(ceil) / 919(nearest)**로 더 갈림; MLAS는 `M=2^k`를 시프트로 특수처리하지 않음 |
| 2026-09-10 | 논문 1 투고 준비 마무리 | 저자 확정·아티팩트 태그 `paper1-v1` 고정·CRLF 435건 정본화 |
| 2026-09-10 | Overleaf 빌드 검증 + 제출 전 소스 정리 | 22쪽·인용 46건 전량 해소; `\pdfoutput=1`을 1행으로(AutoTeX는 앞 5줄만 스캔) |
| 2026-09-16 | **논문 1 arXiv 공개 + 한국어판** | **arXiv:2609.16085**(primary가 `cs.AR`로 재분류) + 한국어판 LaTeX 신규 |
| 2026-09-16 | **CLAUDE.md 정리 + `HISTORY.md` 분리** | 이력 전문을 무손실 아카이브로 이관해 **60,708 → 8,128 B(−86.6%)**; 날짜 역순 2곳·캡스톤 선후·무효 문장·노후 목표 교정 |

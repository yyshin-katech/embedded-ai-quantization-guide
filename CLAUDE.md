## 하네스: AI 양자화 → 임베디드 배포 학습 가이드 제작

**목표:** `guide (1).html`의 단계 구조를 기반으로, Ubuntu 22.04 + NVIDIA RTX 환경에서 실행 가능한 단계별 학습 가이드 MD 세트(`study_guide/`)를 생성/갱신한다.

**트리거:** 양자화·임베디드 가이드 문서의 작성/보완/업데이트/부분수정/재검토 요청 시 `embedded-guide-orchestrator` 스킬을 사용하라. 단순 질문(개념 설명 등)은 직접 응답 가능.

**구성 요소:** 에이전트 `guide-author`(리서치+작성), `tech-reviewer`(검증) / 스킬 `embedded-ai-research`, `stage-guide-writing`, `tech-guide-review`, `md-to-html`(HTML 렌더러) / 오케스트레이터 `embedded-guide-orchestrator`. (상세는 `.claude/agents/`, `.claude/skills/` 참조)

**공통 규약:**
- **커밋·푸시**는 스캔 후 요청 시만 수행. 실기 실측 이력 다수는 사용자 요청으로 수행됨(해당 커밋 해시는 각 행에 기재).
- **실기 실측 캐비앗(불변)**: 절대값(지연·top-1·mAP)은 배치1·서브셋·경로 상이라 **상대 관계만 유효**. 각 행 캐비앗은 개별 특이사항만 기재.
- **부분수정 절차**: 해당 author 몫 직접 수행 → `tech-reviewer` 서브에이전트 팬인 검증(수치 SSOT 1:1·산술·SVG 비례·§승번 오염 0·캐비앗 병기). 결과는 🔴/🟡/🟢 건수로 요약.
- **아래 표의 각 실기 행 산출물**: 실측 리포트 HTML(`logs/`) + 실험 스크립트/결과(`experiments/`) + 필요 시 로컬 메모리. 세부 수치는 리포트·메모리를 정본으로 참조.

**변경 이력:**

| 날짜 | 변경 내용 | 대상 | 사유(핵심 결론) |
|------|----------|------|------|
| 2026-07-27 | 초기 구성 (Fan-out/Fan-in + Producer-Reviewer) | 전체 | 단계별 가이드 MD 제작 하네스 신규 구축 |
| 2026-07-28 | MD 심화 재작성 + `md-to-html` 스킬(HTML 렌더러) 추가 | skills/md-to-html, study_guide/* | 각 단계 상세화 및 HTML 버전 제공 요청 |
| 2026-07-31 | 실측 반영 정정 (부분 재실행) | study_guide/01·02·03·04·05·06·07·09·10, README | 0단계 실설치서 정본 핀 `onnxruntime-gpu==1.28.0` 미존재, `onnx` IR 상한·cuDNN 경로·`numpy<2`·`onnxscript` 누락 드러남 → 실측 스택(ORT 1.23.2 / onnx 1.18.0)으로 전 문서 통일 |
| 2026-07-31 | 0.5단계 TensorRT EP LD_LIBRARY_PATH 픽스 (3-4-a, 부분수정) | study_guide/01_environment_setup.md·html, README | Lv.3 실습 중 `TensorrtExecutionProvider`가 provider 목록엔 나오나 `libnvinfer.so.10`(`tensorrt_libs` 패키지, `nvidia/*/lib` 글롭 밖)을 못 찾아 조용히 CPU 폴백(p50 11.83ms=CPU급 → 픽스 후 0.41ms) → cuDNN과 동일 LD_LIBRARY_PATH 패턴으로 해결, `verify_trt_ep.py`·트러블슈팅 행 추가 |
| 2026-08-02 | 1단계 양자화 이론 실습 반영: 정정 10건 (부분수정) | study_guide/03·05·10 (+html), README 2종, logs/stage1_quantization_log.html | 경험적 단정 다수가 실측과 어긋남 → ① ORT `Entropy`가 기본값(`num_bins=128`)서 후보 1개로 퇴화해 **MinMax와 비트 단위 동일**, `num_bins`는 `quantize_static` 화이트리스트 밖이라 전달 불가 ② "Entropy/Percentile > MinMax" 통념이 ResNet18선 반대(MinMax 78.90% 최적, pct99.9 −6.20%p) — 클리핑 비율↔top-1 단조 ③ ORT 권장 `QUInt8` 비대칭 QDQ를 TRT가 파싱 못 해 무음 폴백(3.05ms) → 대칭 `QInt8` 0.55ms, **하드 블로커=zero-point≠0 하나뿐**(INT32 bias DQ는 2차) ④ layer sensitivity가 "conv1 취약" 통념 반전(21개 중 최안전) ⑤ opset 17 다운컨버트 실패가 **exit 0**으로 통과 ⑥ QAT/STE 기대 출력값 정정 + STE 미적용 대조군 ⑦ 1000장 큐레이션 셋 top-1 부풀림 단서 + McNemar 도입 |
| 2026-08-10 | 인수인계 문서 + 작업 메모리 사본 + QAT 실험 이관 (직접 수행) | HANDOFF.md, .claude/memory/, experiments/qat_recovery/, README | 다른 PC 인계 상태 문서화 → ① `HANDOFF.md` 신설(데이터 27GB·venv 14GB 확보 절차·QAT 재실행법·이전 머신 GPU 고장 진단) ② 로컬 메모리 7건 복사 — 🔴 스캔서 sudo 암호 평문 2곳 마스킹, git 자격증명 메모리 제외 ③ QAT 스크립트 2개 이관 ④ 실측: **배치 축소는 GPU 이탈에 무효**(BS 96→48 전력 −8W뿐), 진짜 시그널 SW Power Cap → 다음 레버 `nvidia-smi -pl` ⑤ 스크립트 버그 3건 수정 |
| 2026-08-06 | 1단계 **50k 재실행** 반영: 정정 12건 + 죽은 링크 3건 + 학습자료 페이지 (직접 수행) | study_guide/02·03·05·06·08·10 (+html), README 2종, learning_resources.html, render.py, .gitignore | ImageNet val **50,000장 전량** 재실행 → 1차(1,000장) 수치가 평균 **+9.77%p 부풀려졌고 Δ 부호 3건·유의성 판정 5건 반전** → ① **함정 0(평가셋 검정력)** 신설(SE/CI·McNemar `eval_power_check.py`·반전표) ② **함정 2-b(전처리)** 신설 — squash vs torchvision 차이 **−1.07%p**가 양자화 대가(−0.12%p)의 ~9배, FP32로 공식값(69.758%) 재현 후 양자화 ③ 함정 4에 CUDA-EP 교차 판정(`trt < cuda×0.8`) ④ `05`: 대칭 강제 대가 "무료(p=0.52)"→**−0.29%p·p=9.2e-5 유의**, `QuantizeBias` 영향 정확히 0 ⑤ `03`: SQNR↔Δtop-1 예측력 없음(Spearman −0.04), per-tensor 격차 −1.20→−0.16%p, 평가 장수 목적별 분기 ⑥ 죽은 링크 3건 교체(TI·LiteRT) ⑦ `learning_resources.html` 신설 ⑧ 렌더러 `slugify` GitHub 규칙 일치(앵커 3건 복구) |
| 2026-08-16 | QAT 회복 실험 완주(W4A8 손실변형) + 보고서 §11 (직접 수행) | experiments/qat_recovery/*, logs/stage1_50k_rerun_reproduction_report.html, HANDOFF.md | 이전 머신 GPU 고장으로 미완이던 실험을 RTX 3080서 완주 → ① 기본 W8A8은 무손실급이라 회복률이 노이즈 → **weight 4-bit 손실변형**(`QAT_WBITS=4`)으로 첫 측정: FP32 68.51%→PTQ 44.35%(**−24.16%p**)→QAT 67.81%=**97.1% 회복** ② 대조군(fake-quant만 제거한 동일 FP32 파인튜닝)이 순수 추가학습 몫 **+0.80%p** 분리 → **QAT−대조군 −1.50%p**가 환원불가 대가("공짜 회복" 반증) ③ 스크립트에 `QAT_WBITS` 노출·로그·JSON·README 완결 ④ 보고서 §11(SVG 2종·판정 4종), 기존 §11→§12 ⑤ RTX 3080은 300W/95%/70°C 완주(Power Cap 없음) → HANDOFF "옮기는 게 해결책" 확인 ⑥ 캐비앗: val split 학습이라 절대 top-1 문헌비교 불가 |
| 2026-08-16 | 1단계 §2.5에 QAT 회복 실측(W4A8) 반영 (부분수정) | study_guide/03_quantization_theory.md·html | QAT 실험(커밋 10461f2) 확정 수치가 §11엔 있으나 본문엔 없어 §2.5가 합성 텐서만 보임 → ① **§2.5.4 신설**(실모델 회복, ResNet18): 회복 계단·대조군 분해 표 2종, "회복은 진짜지만 공짜 아님" 판정, val split 캐비앗, `experiments/qat_recovery`·§11 크로스링크 ② 왜 손실변형: 정본 W8A8 PTQ 손실 노이즈라 회복률 무의미 → `QAT_WBITS=4` ③ LSQ를 2.5.5로 승번 ④ tech-reviewer 팬인 수치 15개·산술 7관계 확인(🔴 0) |
| 2026-08-16 | **2단계 DETR INT8 양자화 실측** (부분수정) | study_guide/04_transformer_quantization.md·html, logs/stage2_detr_quantization_report.html, experiments/stage2_detr/ | 712줄 초안을 RTX 3080 `facebook/detr-resnet-50`으로 완주(COCO val2017 5,000장 mAP) → 단정 **3건 반전** ① export 첫 블로커가 통념의 `grid_sampler`가 아니라 **SDPA**(opset14+)임을 4경로 대조로 확정(DETR엔 grid_sample 없음); torch 2.11 export 기본 dynamo=True(opset 18 강제)·timm 필수 실측 ② §4.5 "폭락→mixed 회복" **반증** — attention matmul 36개 FP로 남겨도 0.2402→0.2438(+0.36뿐), 초안 제외 패턴 대부분 no-op ③ 절제로 **범인=분산** 확정(transformer만 INT8=0.2391≈full, backbone만=0.2653) → op-선택 아닌 **activation 입도**(SmoothQuant/per-token)가 레버 ④ 폭락: FP32 0.4207(공개 42.0)→INT8 0.2402(**−42.9%**), mAP_s −77% ⑤ Percentile 캘리브 3중 실패→MinMax만 생존 ⑥ 캐비앗: CUDA EP·MinMax 상대만. SmoothQuant(§4.4)·BEVFormer(§4.6) 다음 과제 |
| 2026-08-17 | **2단계 §4.6 BEVFormer-tiny grid_sample/Deformable Attention 실측** (부분수정) | study_guide/04_transformer_quantization.md·html, logs/stage2_bevformer_quantization_report.html, experiments/stage2_bevformer/ | 미검증 §4.6 초안을 RTX 3080 `bevformer_tiny`로 완주(2-tier: 무컴파일 레거시 venv mmcv-full 1.7.0/mmdet3d 1.0.0rc6, torch 1.13+cu117이 CUDA 12.8 머신서 하위호환 init) → ① **op 단정 반전 0건**(초안이 맞음) — grid_sample 4D=opset16·5D=opset20·ORT 5D 무커널→무음 CPU 폴백·TRT rank-4 하드단언·MSDeformAttn=grid_sample×num_levels 실측 ② **실전 함정 2건** — (a) 바닐라 mmcv 커스텀 op은 CPU 텐서로만 유효 export(CUDA export는 출력을 Constant baked, exit 0인데 silent-wrong); (b) 전체 모델 export는 `point_sampling`의 `lidar2img.view(...)`서 RuntimeError 사망 — 3층 벽이라 바닐라 유효 export 경로 없음 ③ 폭락 대신 **FP32만**: nuScenes-mini mAP 0.2647/NDS 0.2667 — 81샘플 고분산 스모크라 문헌비교 불가 ④ 전체 INT8=범위 밖(포크 플러그인 툴체인 전제) ⑤ tech-reviewer PASS(🔴 0) |
| 2026-08-17 | **2단계 §4.4 DETR SmoothQuant(nvidia-modelopt) 실측** (부분수정) | study_guide/04_transformer_quantization.md·html, logs/stage2_smoothquant_report.html, experiments/stage2_smoothquant/ | 미검증 §4.4 초안을 RTX 3080 `facebook/detr-resnet-50`·modelopt 0.45.0으로 완주 — §4.5 지목 "레버=activation 입도"를 직접 시험(경로=Design X torch fake-quant, 두 프리셋이 algorithm만 다른 걸 이용한 깨끗한 A/B) → ① 헤드라인(5,000장): per-tensor INT8 폭락 0.4209→0.3301(−0.0908)을 SmoothQuant(α=1.0)가 0.3845=**gap의 59.9%(+0.0544)** 회복 → §4.5 op-선택 mixed(+0.0036)의 **~15배**로 판정 4 확증 ② 초안 오류 3건 — (a) α 프리셋 기본은 논문 0.5가 아니라 **1.0**(설치본 `model_calib.py:954` 실검증), DETR선 α=1.0>0.5 (b) dict-override 유효 실증 (c) absmax 실측 k_proj 3.69×→1.96× ③ 캐비앗: torch fake-quant라 §4.5 ORT QDQ 절대값과 1:1 비교 불가 ④ tech-reviewer PASS(🔴1·🟡1 수정: smooth_check 주석 "30배→3배" 통념이 실측과 충돌 → 모델 의존 명시) ⑤ **2단계 실기 3종(DETR·BEVFormer·SmoothQuant) 완결** |
| 2026-08-17 | **3단계 TensorRT 10.16 pip 휠 실측** (부분수정) | study_guide/05_tensorrt.md·html, logs/stage3_tensorrt_report.html, experiments/stage3_tensorrt/ | 1135줄 초안(전부 trtexec)을 RTX 3080 + TRT 10.16.1.11(pip 휠)로 완주(ResNet50) → ① **헤드라인 반전**: 정본 pip 휠에 **trtexec 실행파일 없음** → 모든 명령 실행 불가 → **polygraphy 0.50.3 Python API**로 3점(FP32 1.6615→FP16 0.8459 **×1.96**→INT8 0.7843ms **×2.12**, −0.52%p) ② §2.2.1 정밀화(반전 아님): 1단계는 "폴백 원인=zp≠0 하나뿐"이었으나 **직접 파서엔 하드 블로커 둘** — INT32 bias DQ(case B: zp=0인데도 parse❌)와 zp≠0(case C: `shiftIsAllZeros`) 독립. 차이는 경로(ORT-EP가 파서 전 bias DQ 흡수), 5케이스 절제(A~E)로 파서 vs 빌더 분리(D: conv1 3ch7×7 parse✅ build❌→`nodes_to_exclude`) ③ **implicit 생존**: deprecated `IInt8EntropyCalibrator2`가 10.16서 빌드(경고 134건), explicit보다 빠르고(0.7074ms **×2.35**)·정확(76.80%)—층별 정밀도 자동선택, 신규는 explicit 권장 ④ 범위 밖: DLA(num_DLA_cores=0)·IPluginV3·modelopt.onnx ⑤ tech-reviewer 🔴1 수정(§2.2.1 "반증"→"정밀화 지점": 병기 프레이밍과 자기모순) |
| 2026-08-18 | **4단계 CPU 폴백 프록시 실측(Raspberry Pi 5 Cortex-A76)** (부분·프록시) | study_guide/06_multi_soc.md·html(§2-2 신설), logs/stage4_arm_cpu_fallback_report.html, experiments/stage5_infrastructure/cpu_proxy/ | 벤더 NPU(보드 대기) 대신 세 SoC 공통 ARM 폴백 경로를 A76 프록시로 실측 → ① **부호 반전**: 같은 ResNet50 INT8 QDQ·같은 ORT CPUEP인데 **CPU ISA가 양자화 이득 부호 결정** — Pi 5 A76(SDOT 있음) INT8 144.95→79.08ms **×1.83 빨라지고**, x86 i9-10900K(VNNI 없음) 9.28→16.34ms **1.76× 느려짐** ② 크로스플랫폼 예측 동일성은 FP32에서만: **FP32 1000/1000·INT8 958/1000**(정수 누산·재양자화 반올림이 ISA 민감) ③ 부수: golden은 결정론적 TRT 전용 → CPU 4행을 `cpu_proxy/results/`로 격리(하네스 6행·pytest 3 passed 유지) ④ 정직 폴백: Pi는 자동차 NPU 아님, **offload 0% 바닥값**(가속기가 이겨야 할 최소선) ⑤ tech-reviewer PASS(🔴 0) ⑥ 캐비앗: Pi↔x86 절대속도는 다른 급 기계라 논점 아님 |
| 2026-08-17 | **5단계 벤치 하네스(polygraphy Python API) 실측** (부분수정) | study_guide/07_infrastructure.md·html, logs/stage5_infrastructure_report.html, experiments/stage5_infrastructure/(bench/*) | 1229줄 초안을 RTX 3080 + TRT 10.16.1.11로 완주(ResNet50) → 정정 8건 ① trtexec 부재(3단계 동일)→`TrtRunner`, 3점(FP32 1.837→FP16 1.0231 **×1.80**→INT8 0.8628ms **×2.13**) ② **무음 오답**: `TrtRunner.infer`가 출력 host 버퍼 재사용 → `.copy()` 없이 모으면 top-1 **0.0014 붕괴**(pycuda→polygraphy 치환이 도입)→`.copy()`로 복원 ③ **무음 오답**: pivot `dropna=True`가 all-NaN 스텁행 드롭(§5-1 회색행 원칙 위반)→`dropna=False`(CSV·회귀 baseline 무영향) ④ deprecated 캘리브레이터 10.16서 빌드 ⑤ 작동 attr=`device_memory_size_v2`, 값은 **scratch mem≠엔진파일 크기** ⑥ `EXPLICIT_BATCH` flag 10.16서 값=0 deprecated ⑦ `data.py` eval-set 정정 ⑧ "pytest-regressions v3.0+" 허구→실제 2.11.0(코드 정상·버전 주장만 틀림) ⑨ 모델 스코핑: BEVFormer 대신 ResNet50(BEVFormer INT8 유효 export 없음) ⑩ tech-reviewer PASS(🔴 0) ⑪ 캐비앗: 하네스 wall-clock이라 3단계 event-timed 대비 factor 압축 |
| 2026-08-18 | **4단계 Qualcomm 벤더-NPU 실측(AI Hub 클라우드 Hexagon HTP)** (부분·클라우드) | study_guide/06_multi_soc.md·html(§4-B 콜아웃), logs/stage4_qualcomm_aihub_report.html, experiments/stage4_qualcomm_aihub/ | CPU 폴백 프록시(offload 0%) 위에 벤더 NPU 실측을 보드 없이 채움(qai_hub 0.54.0/HTP v73) → ① 헤드라인: ResNet50을 HTP 두 종(QCS8550·SA8775P ADP)에 올려 둘 다 **100% NPU offload**, INT8 fp16 대비 **×1.77/×2.03** ② 🔴 **무음 오답**: 외부 ORT-QDQ 지참 시 compile/profile 통과해도 on-device top-1 **0.75→0.005 붕괴**(HTP가 외부 QDQ scale 무시, FP32는 0.745 충실) → 올바른 경로=AI Hub 자체 `submit_quantize_job`(HTP-native QDQ)로 **0.735 회복**, 게다가 748µs로 외부-QDQ INT8 1052µs보다 빠르고 leaner ③ 발견 3종: value_info↔IO 엄격 거부·HTP native fp32 없음(fp16 실행)·엄격 NCHW ④ 정직 범위: Qualcomm 전용, TI/Renesas 보드 대기 ⑤ tech-reviewer PASS(🔴0·🟡1 해소) ⑥ 캐비앗: 200장 서브셋, native 0.735도 ORT 0.750보다 낮아 공짜 아님 |
| 2026-08-18 | **8단계 캡스톤 INT8/TRT 관통(후속 세션)**: BEVDet 커스텀 TRT-8.5-plugin 6벽(W1~W6) + 지연 사다리 (부분·직접) | logs/stage8_capstone_int8_report.html(신규)·stage8_capstone_report.html(정정 5곳), study_guide/08_capstone.md·html, experiments/stage8_capstone/ | 1차 세션(커밋 64e4c84)이 남긴 경로 A1(`convert_bevdet_to_TRT.py --int8`, §4.6과 동일 포크 플러그인 벽)을 user-space에 조립해 관통 → ① **진짜 벽은 W3·W5**(W6은 벽 아님): W3 커스텀 `TRTBEVPoolV2` 플러그인을 풀 CMake 없이 2개 TU만 self-contained nvcc/g++ 직접 컴파일; W5 `torch.onnx.export`가 nested `QuickCumsumCuda`서 RuntimeError+segfault → export 전용 `new_zeros` shim monkeypatch(엔진 정확도 무영향, 표준 mmdeploy 기법); W6 deprecated `build_engine` segfault=API 탓 → `build_serialized_network` ② 지연 사다리(batch1 event-timed): FP32 14.680→FP16 4.905(×2.99)→INT8 2.630ms(**×5.58**), 엔진 245.0/90.1/46.7MB, INT8 캘리브 ENTROPY_2·nuScenes-mini 81장 ③ FP32 대비 출력편차(6 head): FP16 corr≥0.9994·무시, INT8 corr 0.985~1.000 ④ 교차확증: TRT FP32 14.68ms가 1차 PyTorch eager 34.06ms보다 2.3× 빠름 ⑤ tech-reviewer PASS(🔴0·🟡1 해소) ⑥ 캐비앗: init 가중치(Baidu-locked)라 절대 mAP≈0 무의미 — 유효 산출물은 지연·엔진 크기·출력편차뿐, 플러그인·엔진은 legacy env(`~/bevf-legacy`) 전용 정본 오염 0 |
| 2026-08-18 | **8단계 캡스톤 FP32 walking skeleton 관통(제3의 길 user-space cu117)** (부분·직접) | study_guide/08_capstone.md·html(§3·§4-1·§4-2·§4-4·§9 콜아웃), logs/stage8_capstone_report.html, experiments/stage8_capstone/ | 794줄 초안을 AI-LAP RTX 3080서 관통 — 중간 스코프(사용자 사전합의=FP32 baseline까지) → ① **헤드라인=제3의 길**(문서 §3 빈칸): BEVDet `bev_pool_v2` 커스텀 CUDA op은 torch≤1.13+CUDA≤11.7 요구인데 머신 toolkit은 CUDA 12.8 → hard-error(§4.6과 동일 근본). 문서 §3 2안(Docker/blackwell) 둘 다 막힘 → **user-space cu117 툴체인 손조립**(micromamba `cuda-nvcc=11.7` + pip `nvidia-cuda-runtime-cu11` 실제 `libcudart.so.11.0` + `CUDA_HOME` 조립) → `bev_pool_v2_ext.so` 빌드 성공(핵심=`-lcudart` 실제 SONAME 해결) ② 2차 벽 spconv: bundled mmdet3d가 LiDAR-fusion DAL eager import → `spconv-cu117`로 해소 ③ 데이터 벽: `create_data_bevdet.py` 3곳 패치(`v1.0-mini`), 323 train/81 val. 초안 오류: 태그가 `bevdetv2`가 아니라 **`bevdetv3`** ④ walking skeleton 완주 + 정직 폴백: 정식 가중치 Baidu Pan 전용(미러 못 찾음)→init 가중치로 stock `tools/test.py` 관통→**mAP 0.0000/NDS 0.0260은 버그 아니라 예상값**(backbone만 진짜). latency는 가중치 무관 유효: FP32 forward **p50 34.06ms**(공식 33.3ms 근접 교차확증) ⑤ INT8/TRT-plugin 범위 밖(§4-4 3경로 모두 벽) ⑥ tech-reviewer PASS(🔴0·🟡1: 공개 R50 §2-1 0.283/0.350 vs §4-2 0.298/0.379 병존→§2-1 각주 "config별 상이" 화해) |
| 2026-08-20 | **3·4단계 Jetson AGX Orin 온디바이스 실측**: iGPU 정밀도 사다리 + DLA + tegrastats + §2-2 A78AE 4행 (부분·온디바이스) | study_guide/05_tensorrt.md·html(§2.3)·06_multi_soc.md·html(§2-2 4행+신설 §2-3), logs/stage3_jetson_orin_ondevice_report.html, experiments/stage3_tensorrt/jetson_ondevice/ | RTX 프록시로 불가였던 온디바이스를 실 Orin(JetPack 6.2.1·TRT 10.3.0.30·2×NVDLA v2·MAXN)서 관통 → ① 3·5단계 "trtexec 부재→polygraphy" 해소(JetPack 동봉 실 trtexec, 정정 `--buildOnly`→`--skipInference`) ② 3단계 "DLA 범위 밖" 해소: ResNet50 5엔진 — iGPU FP32/FP16/INT8(1.9375/1.0293/1.0132ms, 작은 Ampere라 **INT8≈FP16 0.984** vs RTX3080 0.927) + DLA ③ **DLA INT8=성능/와트 챔피언 51.29 inf/s/W**(iGPU INT8의 ×1.547·전력 0.511×) ④ **DLA는 INT8 전용기**: 배치 INT8·FP16 동일한데 DLA FP16 13.87× 느림=순수 NVDLA v2 데이터패스 ⑤ 오프로드 계측: GR3D iGPU 95%→DLA 3~16%(DLA는 GR3D에 안 잡힘) ⑥ **§2-2 sign-flip 4번째 행**: A78AE 38.47→18.22ms **×2.11**(2번째 dotprod-ARM, Pi5 확증, 커밋 49e30ff) + 크로스플랫폼 정밀화 — 같은 MLAS SDOT면 INT8 예측 비트 동일(Jetson↔Pi5 1000/1000), 다르면 ~96% ⑦ 신설 06 §2-3 ⑧ tech-reviewer PASS(🔴0·🟢2) ⑨ 캐비앗: DLA INT8은 암묵 캘리브라 지연·전력만·정확도 미주장, Jetson은 NVIDIA edge이지 자동차 벤더 아님(데이터 00fd97d·문서 1a8f073) |
| 2026-08-21 | **Jetson 후속**: iGPU∥DLA 동시부하 + nvpmodel 전력 스윕 (부분·온디바이스) | study_guide/05_tensorrt.md·html(§2.3)·06_multi_soc.md·html(신설 §2-4), logs/stage3_jetson_orin_concurrent_power_report.html, experiments/stage3_tensorrt/jetson_ondevice/ | 00fd97d 캐비앗 "동시부하 미측정"을 닫고 전력 축 추가 → ① **동시부하=공짜 병렬 아님(GPU-폴백 직렬화)**: iGPU+DLA0=ideal의 **60.8%뿐**(DLA 27% 붕괴·iGPU 87.6% 유지), 범인=DLA 엔진 GPU-폴백 2층(GlobalAveragePool+flatten)이 포화 iGPU 큐 뒤 직렬화 ② DLA0+DLA1(GPU 유휴)=**87.0%(1351.5 qps) + 최고 perf/watt 66.07** ③ 3-way(1197.2)<2-DLA(1351.5): 바쁜 GPU가 두 DLA 폴백 교살 → **설계규칙: 진짜 병렬은 DLA 서브그래프 GPU-폴백 0층일 때만** ④ 전력 스윕=리더 교차: MAXN→50W서 iGPU **−29.4% 급락** vs DLA **−2.9%** → 50W서 DLA +8.8% 빠름, DLA ppw 상시 우위(×1.55~×1.47) ⑤ 30W/15W=재부팅 게이트(사용자 거부)라 미측정 ⑥ tech-reviewer PASS(🔴0) ⑦ 캐비앗: GPU-폴백 직렬화 결론은 모델 의존(커밋 c03c174) |
| 2026-08-21 | **Jetson 새 모델 축(DETR)**: iGPU 정밀도 사다리 + case C 파싱 실패 + DLA 파편화 (부분·온디바이스) | study_guide/05_tensorrt.md·html(§2.3), logs/stage3_jetson_orin_detr_report.html, experiments/stage3_tensorrt/jetson_ondevice/detr/ | ResNet50 사다리(00fd97d) 위에 트랜스포머(`facebook/detr-resnet-50`)를 같은 보드·trtexec로 올림 → ① **무거운 트랜스포머 ⇒ INT8 이득 부활(모델 의존)**: DETR FP16 ×1.947·INT8(implicit) **×2.742**. ResNet50@224는 launch/memory-bound라 INT8≈FP16(0.984 무이득)이었으나 DETR@800×1066은 큰 attention matmul로 compute-bound → "작은 iGPU는 INT8 무이득"은 iGPU가 아니라 **가벼운 CNN의 성질**(00fd97d 반증 아닌 정밀화) ② **explicit-QDQ INT8 파싱 실패(case C 실모델 재현)**: `detr_int8.onnx`(zp≠0+INT32 bias DQ)가 node 0서 `shiftIsAllZeros` 실패 → accuracy-valid INT8은 QDQ 수술/대칭 재export 없이는 막힘 ③ **트랜스포머가 DLA 파편화(헤드라인)**: ResNet50에 2/2 offload를 준 동일 플래그가 DETR엔 **326 DLA/404 GPU-폴백/16 ForeignNode**(NVDLA v2 코어당 16 subgraph 상한) → DLA FP16 398.64ms=iGPU FP16의 **30× 느림** → **설계규칙: DLA는 CNN 가속기, 트랜스포머는 iGPU** ④ 정확도 미측정(보드에 COCO 없음)·2단계 RTX 값 인용 ⑤ tech-reviewer PASS(🔴0·🟡1 해소: 9~13×가 지연 비인데 엔진크기 열 옆이라 오독→"지연이 9~13×" 정정)(커밋 9ef2a58) |
| 2026-08-21 | **Jetson 정확도 축**: iGPU explicit-QDQ INT8 정확도 + TRT INT8 vs CPU MLAS 예측 1:1 (부분·온디바이스) | study_guide/05_tensorrt.md·html(§2.3)·06_multi_soc.md·html(§2-3), logs/stage3_jetson_orin_accuracy_report.html, experiments/stage3_tensorrt/jetson_ondevice/accuracy/ | 솔로 스윕의 "정확도 미주장"을 닫음 — 저장 `.plan`을 CPU-프록시 같은 ResNet50 1000장 번들에 돌려 예측 1:1(결정타: `rn50_gpu_int8.plan`이 CPU 프록시와 같은 `resnet50_int8_qdq.onnx`서 빌드→QDQ scale 동일→유일 변수=정수커널) → ① **accuracy-valid INT8이 온디바이스 유지 + CPU 커널 이김**: iGPU INT8(explicit QDQ) top-1 **0.7620=FP32 무손실 > CPU MLAS 0.7500**(무음 FP32 폴백 아님: 자기 FP32와 57장 상이) ② **INT8 경로의존이 CPU↔가속기 경계 넘음**: TRT INT8 vs MLAS INT8=**961/1000**(4단계 Jetson↔x86 958 대역, ≠Pi5 100% 같은 커널), net +12. FP32는 경로 무관 1000/1000 ③ **implicit DLA INT8=0.017 붕괴**(같은 하네스가 DLA FP16 0.7610→하네스 버그 아님) ④ 부수: "top-1 불변 ≠ 예측 불변"(INT8 vs 자기 FP32 57장 플립이나 net-neutral) ⑤ tech-reviewer PASS(🔴0·🟡1 수정: DETR 콜아웃 line-ref 876→892) ⑥ 캐비앗: 1000장이라 5000장 RTX 비교 불가, 같은 번들 상대만(커밋 d563439 푸시완료) |
| 2026-08-21 | **Jetson 정확도 축 후속(DETR)**: 대칭 재양자화로 case-C 우회 → explicit-QDQ INT8 정확도유효 엔진 + COCO mAP (부분·온디바이스) | study_guide/05_tensorrt.md·html(§2.3), logs/stage3_jetson_orin_detr_accuracy_report.html, experiments/stage3_tensorrt/jetson_ondevice/detr_accuracy/ | DETR 새 모델 축(9ef2a58)이 case C로 남긴 "정확도 미측정"을 대칭 재양자화로 닫음(COCO head 1000·fixed 800×1066·pycocotools) → ① **두 벽→툴체인 언락**: (1) case C(zp≠0)+case B(INT32 bias DQ)를 대칭 재export로 제거(`ActivationSymmetric+WeightSymmetric`·`QuantizeBias=False`·conv1 제외), (2) 트랜스포머 2차 벽(빌더): self-attention quantized-const 거부 → `op_types_to_quantize=[Conv,Gemm]`(attention/LayerNorm/Softmax는 FP16, 2단계 §4.5 정합) → build-failed였던 explicit INT8 빌드됨 ② **헤드라인(가설 반전) — case-C 픽스는 툴체인 언락이지 정확도 구제 아님**: accuracy-valid explicit-sym INT8이 stage2 폭락 재현 FP32 0.4237→0.2383(**−43.8%**)·mAP_s −84.6%, stage2(ORT/dynamic/5000) −42.9%와 **교차확증** → 레버는 sym/case-C가 아니라 **activation 입도(§4.4)** ③ implicit `--int8` 정정: implicit 0.4073이 sym보다 높다고 "더 나은 INT8" 아님(무통제 미주장), 단 순수 FP16 폴백도 아님(INT8 커널 실제 실행) ④ 지연: FP32 25.85→FP16 13.28→sym 11.002(×2.35 accuracy-valid)→implicit 9.43ms ⑤ tech-reviewer PASS(🔴1·🟡3 전부 fixed: 🔴 sym 엔진크기 단위모순 46.9↔44.69 MiB→44.69 통일) ⑥ 캐비앗: fixed·TRT·1000장이라 stage2와 1:1 비교 불가, 상대 폭락만(그게 교차확증). **DETR "정확도 미측정" 닫힘**, 다음 후보=SmoothQuant/per-token(커밋 58ba518 푸시완료) |
| 2026-08-21 | **Jetson 정확도 축 후속2(DETR SmoothQuant)**: activation 입도 레버를 ONNX-레벨로 온디바이스 시험 (부분·온디바이스) | study_guide/04_transformer_quantization.md·html(§4.4), logs/stage3_jetson_orin_detr_smoothquant_report.html, experiments/stage3_tensorrt/jetson_ondevice/detr_smoothquant/ | 후속4(58ba518) 지목 레버(activation 입도)를 실 Jetson서 ONNX-레벨 SmoothQuant로 시험 — stage2 §4.4는 59.9% 회복했으나 torch 경로(Design X)에서만·온디바이스 미측정 → ① **헤드라인(교차확증 반전) — 온디바이스는 폭락의 ~9%만 회복, stage2의 1/6**: gap 0.1854 중 α=1.0 **9.2%(0.2553)**·α=0.5 8.7%. **근본원인=op 커버리지**(SmoothQuant가 틀린 게 아님) — 빌더 벽으로 유일 빌드가능 INT8이 **Gemm 전용**, attention/LayerNorm/Softmax FP16이라 Gemm 입력 smoothing이 지배 잔차 레버 아님; stage2 torch는 모든 linear가 INT8이라 크게 먹힘 → **§4.4 59.9%는 부정 아니라 "경로·op-커버리지 의존"으로 정밀화** ② 방향은 옳음: α=1.0>α=0.5(stage2 순서 보존)·mAP_s 6.1% 부분회복·거의 지연중립(+0.34ms)·absmax 최대 46.699× 감쇠 ③ 툴체인 교훈: modelopt.onnx 임포트 불가→수동 SmoothQuant(95 Gemm, per-input-ch `s=a^α/w^(1−α)`). FP32 게이트는 CPU EP(CUDA TF32가 재스케일에 허위 발화) ④ tech-reviewer PASS(🔴0·🟡1 fixed) ⑤ 캐비앗: stage2와 1:1 비교 불가, 상대 회복률만. **stage2 §4.4 "온디바이스 미측정"+"SmoothQuant 재측정" 둘 다 닫힘**, 다음 후보=attention까지 INT8 가능한 툴체인(커밋 b257a17 푸시완료) |
| 2026-08-31 | **4단계 벤더-NPU 실기기 셋업**: 랩 Pi 5에 DEEPX DX-M1(M.2 PCIe NPU) 설치·설정 (직접 수행) | .claude/memory/README.md, (로컬 전용 미커밋) dx-m1-npu-setup 메모리 | 랩 Pi 5(A76·Debian 13·커널 6.18.34·Python 3.13)에 물리 장착된 DX-M1을 `dx-all-suite`로 사용가능화 → ① dx-runtime 서브모듈만 init(dx-compiler는 x86 전용)→`install.sh`(드라이버 DKMS→검증→dx_rt,dx_fw) ② **최대 리스크 커널 6.18 호환 해소**: 드라이버 소스가 RPi 커널 6.18.34에 클린 컴파일, DKMS AUTOINSTALL로 커널 업데이트 시 자동 재빌드 ③ Python 3.13: DEEPX `dx_engine-3.4.2-cp313` aarch64 휠 제공 ④ 검증(재부팅 생존): `dxrt-cli -s`=M1 Accelerator·FW v2.7.4·NPU 3코어 @1000MHz ⑤ 관련성: 4단계 벤더-NPU 실 온디바이스(같은 A76이 CPU 프록시이자 DX-M1 호스트) ⑥ 범위: 설치·검증만(실추론엔 `.dxnn`, x86서만 컴파일) ⑦ 보안: 내부 IP 참조라 **로컬 전용**, 임시 NOPASSWD sudo 드롭인 제거·재부팅 후 암호 sudo 복원 |
| 2026-08-31 | **4단계 DEEPX DX-M1 벤더-NPU 온디바이스 실측**: CPU↔NPU 오프로드·host-bound·perf/watt (부분·온디바이스) | study_guide/06_multi_soc.md·html(§4-D 콜아웃), logs/stage4_deepx_dxm1_report.html, experiments/stage4_deepx_dxm1/ | 직전 셋업이 남긴 "벤치 미수행"을 완주 — 결정타=같은 A76이 CPU 폴백 프록시(offload 0%)이자 DX-M1 호스트 → 같은 모델(`yolo26n` 640)로 CPU FP32 ↔ NPU INT8 뺄셈(NPU는 INT8 전용) → ① 헤드라인: NPU async 3-core **91.51 fps** vs A76 FP32 4T **8.01 fps=처리량 ×11.42**; E2E 지연 NPU 29.43 vs CPU 124.84ms **×4.24**, NPU 순수 연산만 8.86ms→**×14.08**; perf/watt host-side **×29.29** ② **host-bound(천장은 DX-M1 아니라 Pi 5)**: async 1/2/3-core=91.5 평평(코어 스케일 1.00). 온보드 프로파일러: 3코어 잡 분포 695/427/2·**D2H 출력전송 21.8ms ≫ 연산 8.86ms** → 범인 **PCIe Gen2×1**(네이티브 Gen3×4 대비 ~8× 손실) ③ **전력 계측 갭**: M.2 카드가 PMIC 상류 EXT5V서 끄는 전력 미포착(Orin GR3D↔DLA와 동류) → perf/watt 두 경계 병기(host-side ×29.29·카드 TDP 포함 ×13.1/×9.57, 어느 경계든 10배+ 승) ④ 06 §4-D 콜아웃·크로스링크 3개(§2-2·§2-3·§4-B) ⑤ tech-reviewer PASS(🔴0) ⑥ 캐비앗: prebuilt·배치1 상대만, host-bound는 Pi5 Gen2×1 탓, **정확도 미측정=다음 축**, 로컬 전용 메모리(커밋 f10b4ae 푸시완료) |
| 2026-08-31 | **4단계 DEEPX DX-M1 정확도 축**: native-QDQ vs 외부-QDQ 컴파일 거부 + CPU↔NPU 예측 일치 (부분·온디바이스) | study_guide/06_multi_soc.md·html(§4-D 정확도 콜아웃), logs/stage4_deepx_dxm1_accuracy_report.html, experiments/stage4_deepx_dxm1/accuracy/ | 직전 벤치(f10b4ae) 캐비앗 "정확도 미측정"을 같은 보드·ResNet50+ImageNet 1000장으로 닫음(사용자 (a)+(b)) → ① **(a) 외부 QDQ=컴파일 하드 거부(Qualcomm과 정반대 실패양식)**: `resnet50_int8_qdq.onnx`를 dx_com에 먹이면 `GraphStructureError: 106 isolated node(s)`→`InternalError`, `.dxnn` 미산출; native(FP32+dx_com 자체 PTQ)는 컴파일 OK→top-1 **0.7660**. Qualcomm HTP는 외부 QDQ 조용히 무시(0.75→0.005 붕괴), DEEPX는 시끄럽게 거부(조용히 깨진 모델 배포 불가라 안전) — 공통근본 **벤더 NPU가 양자화 소유(BYO-QDQ 미지원)** ② **(b) CPU↔NPU 예측 939/1000**=정수커널 경로의존을 실 vendor-NPU로 확장(단조 1000→961→939, 939<961은 (a)로 scale 고정불가라 3중 confound), net +16 ③ .dxnn 입력 계약: 전처리 항상 fold→런타임 uint8 NHWC. ⚠️함정 python DataLoader 경로는 잘못된 normalize fold→top1=0, accuracy-valid는 CLI config 경로 ④ 06 §4-D(정확도) 콜아웃·부모 README 캐비앗 #4 해소 ⑤ tech-reviewer PASS(🔴0·🟢3) ⑥ 캐비앗: 1000장 상대만, npu 0.7660>fp32 0.7620은 이김 아님(노이즈), 로컬 전용 메모리(커밋 6f5ae61 푸시완료) |
| 2026-09-01 | **4단계 DEEPX DX-M1 크로스오버 축**: host-bound↔NPU-bound (모델만 바꿔 병목 regime 갈림) (부분·온디바이스) | study_guide/06_multi_soc.md·html(§4-D 크로스오버 콜아웃), logs/stage4_deepx_dxm1_crossover_report.html, experiments/stage4_deepx_dxm1/crossover/ | 벤치(f10b4ae) 캐비앗 ③("host-bound은 Pi5 Gen2×1 탓이지 DX-M1 천장 아님")을 **반대 사례로 닫음** — 같은 DX-M1·같은 Pi5·같은 dxrun에서 **모델만 바꿔** 병목 스테이지 크로스오버 → ① **헤드라인=천장은 하드웨어 아닌 모델(출력 크기)**: resnet50(출력 4,000B) **compute-bound**(연산 2.77ms≫D2H 0.11ms)→3코어 **2.19× near-linear**·잡 961/910/1010 vs yolo26n(출력 2,822,400B raw head) **D2H-bound**(D2H 21.81ms≫연산 9.00ms)→**1.00× 평평**·잡 472/28/2(공유 PCIe가 코어1·2 굶김) ② **변수 격리(결정타)**=YOLOV5S(출력 5,483,520B·연산 **2.59ms 최경량**인데 **26.3× 느림**) ⇒ regime을 정하는 건 연산 아니라 **출력(D2H) 크기** ③ 비율: 출력 y26/rn50 705.6×·D2H 199.7×·async3c rn50/y26 11.83× ④ 방법 교훈: dxrun `-n`은 코어 개수 아닌 ID(카운트 스윕=`-n 1/4/0`) ⑤ tech-reviewer PASS(🔴0) 적용2(🟡 "선형"→near-linear[2.19/3.0=73% sublinear]·🟢1 YOLOV5S 입력 384×672→**512×512** 사실정정[786432≠384×672, dx_engine 실검증]) ⑥ 캐비앗: 2 regime 존재 증명이지 전이 곡선 아님(중간 출력 미측정), 로컬 전용 메모리(검출 축과 함께 **커밋 274bffa 푸시완료**) |
| 2026-09-01 | **4단계 DEEPX DX-M1 검출 축**: yolo26n COCO mAP INT8 무손실급 + NMS-fold 레짐 (정확도·크로스오버를 하나의 검출 모델로 연결) (부분·온디바이스) | study_guide/06_multi_soc.md·html(§4-D 검출 콜아웃), logs/stage4_deepx_dxm1_detection_report.html, experiments/stage4_deepx_dxm1/detection/ | 정확도 축(6f5ae61)이 남긴 "yolo26n mAP 미측정" + 크로스오버 축(위) 둘을 **하나의 검출 모델**로 연결 — 같은 yolo26n@640으로 COCO val2017 500장 mAP(정확도)와 온보드 스테이지 프로파일(레짐) 동시 측정(비트동일 letterbox npy md5 984e0457…+공유 `[1,300,6]` 디코드 → mAP 차=양자화 only) → ① **헤드라인(1) 검출 INT8 무손실급**: FP32 0.448→동일-weight COCO-캘리브 INT8(Option B) 0.439=순수 양자화 **−0.009(−2.0%)**, 캘리브 도메인(B−A) +0.0033 노이즈 내, 소객체 mAP_s 0.2283→0.2144 **−6.1%**(손실 집중되나 경미, stage2 DETR mAP_s −77~85% 폭락과 차원 다름) → 정확도 축 분류 무손실급(native 0.766≈FP32 0.762)의 **검출판 확증** ② **헤드라인(2) NMS-fold는 레짐 못 바꿈(가설 반증)**: 가설=`[1,300,6]` end2end가 출력 2.82MB→7.2KB(392×) 줄여 compute-bound로 뒤집음 → **반증** — dx_com이 raw multi-scale 헤드(6 conv `one2one_cv2/cv3` @80/40/20=**2,822,400B**, 바이트검산 정확)서 그래프 절단, decode+NMS는 **호스트(CPU)** 실행(.dxnn 번들)→매 프레임 2.82MB PCIe 건넘→여전히 **D2H-bound**(D2H 21.805ms≫연산 7.303ms=2.99×·잡 473/27/2) → 크로스오버 raw-head yolo26n(472/28/2·D2H 21.81)과 **동일 시그니처**=크로스오버 결론 **반증 아니라 확증·심화**(D2H 병목은 export 우연 아니라 이 툴체인에 NMS-fold 길 없다는 **구조적** 사실), "그냥 NMS 접어 출력 줄이면" 순진한 가정 닫음 ③ 방법·함정: Option B=`dxcom -c yolo_coco_cfg.json` 동일 stock onnx+100장 in-domain COCO 캘리브(val2017[500:600]⊥eval[0:500])→confound 0; ⚠️**yolo 캘리브는 ÷255만**(ResNet50은 div+ImageNet norm)→config `normalize` 제거 필수·런타임 uint8 NHWC RGB ④ tech-reviewer PASS(🔴0·🟡0·🟢2) ⑤ 캐비앗: 500장 서브셋 상대 Δ만·prebuilt/batch1/Pi5 Gen2×1(D2H-bound=링크 성질)·host-timed≠순수연산·Option B 컴파일경고 cosine 0.927@output0는 surgeon 근사(mAP엔 −0.009만)·로컬 전용 메모리(**①크로스오버·②검출 함께 커밋 274bffa 푸시완료**, predictions_*.json 3종 21MB는 .gitignore·스크립트 재현) |
